// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/popup/flint-popup.component.ts
//
// Event naming: onFlint[ComponentName][EventType] (e.g. onFlintPaginationChange).
// The props interface is strict — misspelled event names like onFlintPageChange
// will cause a TypeScript error. No index signature is used.

import React from 'react';
import { createComponent, type EventName } from '@lit/react';
import { FlintPopup as FlintPopupElement } from '@getufy/flint-ui/popup/flint-popup';
import { FlintPopupEvents } from '../events/flint-popup.js';

/**
 * flint-popup
 *
 * @slot (default) - The popup content to be positioned.
 * @slot anchor - The reference element the popup is positioned relative to.
 */
export interface FlintPopupProps extends React.HTMLAttributes<FlintPopupElement> {
    /** Activates positioning. When false, Floating UI listeners are torn down. */
    active?: boolean;
    /**
     * The anchor element. Pass a CSS selector string (resolved from ownerDocument),
     * Type: `string | Element`
     */
    anchor?: FlintPopupElement['anchor'];
    /**
     * Preferred placement of the popup relative to its anchor.
     * Allowed values: 'top' | 'right' | 'bottom' | 'left' | 'top-start' | 'top-end' | 'right-start' | 'right-end' | 'bottom-start' | 'bottom-end' | 'left-start' | 'left-end'
     */
    placement?: 'top' | 'right' | 'bottom' | 'left' | 'top-start' | 'top-end' | 'right-start' | 'right-end' | 'bottom-start' | 'bottom-end' | 'left-start' | 'left-end';
    /**
     * CSS positioning strategy. Use `'fixed'` to escape overflow containers.
     * Allowed values: 'absolute' | 'fixed'
     */
    strategy?: 'absolute' | 'fixed';
    /** Distance from the anchor on the main axis (px). */
    distance?: number;
    /** Offset along the cross axis (px). */
    skidding?: number;
    /** Flip the popup when it would overflow the boundary. */
    flip?: boolean;
    /** Space-separated fallback placements when flipping (e.g. `"bottom-start bottom-end"`). */
    flipFallbackPlacements?: string;
    /**
     * Boundary element(s) for the flip middleware.
     * Type: `Element | Element[] | undefined`
     */
    flipBoundary?: FlintPopupElement['flipBoundary'];
    /** Padding from the flip boundary (px). */
    flipPadding?: number;
    /** Shift the popup to stay within the boundary. */
    shift?: boolean;
    /**
     * Boundary element(s) for the shift middleware.
     * Type: `Element | Element[] | undefined`
     */
    shiftBoundary?: FlintPopupElement['shiftBoundary'];
    /** Padding from the shift boundary (px). */
    shiftPadding?: number;
    /**
     * Constrain popup dimensions to available space: `'horizontal'`, `'vertical'`, or `'both'`.
     * Allowed values: '' | 'horizontal' | 'vertical' | 'both'
     */
    autoSize?: '' | 'horizontal' | 'vertical' | 'both';
    /**
     * Boundary element(s) for auto-size.
     * Type: `Element | Element[] | undefined`
     */
    autoSizeBoundary?: FlintPopupElement['autoSizeBoundary'];
    /** Padding from the auto-size boundary (px). */
    autoSizePadding?: number;
    /**
     * Sync popup width, height, or both to match the anchor element.
     * Allowed values: '' | 'width' | 'height' | 'both'
     */
    sync?: '' | 'width' | 'height' | 'both';
    /** Show the built-in arrow pointing toward the anchor. */
    arrow?: boolean;
    /** Arrow padding from the edges of the popup (px). */
    arrowPadding?: number;
    /** Render a transparent bridge between anchor and popup for safe hover transitions. */
    hoverBridge?: boolean;
    /**
     * Fired after the popup is repositioned. `detail: { placement }`.
     * DOM event: `flint-reposition`
     */
    onFlintReposition?: (event: CustomEvent) => void;
}

export const FlintPopup = createComponent({
    tagName: 'flint-popup',
    elementClass: FlintPopupElement,
    react: React,
    events: {
        onClick: 'click' as EventName<MouseEvent>,
        onDoubleClick: 'dblclick' as EventName<MouseEvent>,
        onContextMenu: 'contextmenu' as EventName<MouseEvent>,
        onMouseDown: 'mousedown' as EventName<MouseEvent>,
        onMouseUp: 'mouseup' as EventName<MouseEvent>,
        onMouseEnter: 'mouseenter' as EventName<MouseEvent>,
        onMouseLeave: 'mouseleave' as EventName<MouseEvent>,
        onMouseMove: 'mousemove' as EventName<MouseEvent>,
        onMouseOver: 'mouseover' as EventName<MouseEvent>,
        onMouseOut: 'mouseout' as EventName<MouseEvent>,
        onKeyDown: 'keydown' as EventName<KeyboardEvent>,
        onKeyUp: 'keyup' as EventName<KeyboardEvent>,
        onFocus: 'focus' as EventName<FocusEvent>,
        onBlur: 'blur' as EventName<FocusEvent>,
        onInput: 'input' as EventName<Event>,
        onChange: 'change' as EventName<Event>,
        onSubmit: 'submit' as EventName<Event>,
        onReset: 'reset' as EventName<Event>,
        onScroll: 'scroll' as EventName<Event>,
        onWheel: 'wheel' as EventName<WheelEvent>,
        onTouchStart: 'touchstart' as EventName<TouchEvent>,
        onTouchEnd: 'touchend' as EventName<TouchEvent>,
        onTouchMove: 'touchmove' as EventName<TouchEvent>,
        onTouchCancel: 'touchcancel' as EventName<TouchEvent>,
        onPointerDown: 'pointerdown' as EventName<PointerEvent>,
        onPointerUp: 'pointerup' as EventName<PointerEvent>,
        onPointerMove: 'pointermove' as EventName<PointerEvent>,
        onPointerEnter: 'pointerenter' as EventName<PointerEvent>,
        onPointerLeave: 'pointerleave' as EventName<PointerEvent>,
        onPointerOver: 'pointerover' as EventName<PointerEvent>,
        onPointerOut: 'pointerout' as EventName<PointerEvent>,
        onPointerCancel: 'pointercancel' as EventName<PointerEvent>,
        onDrag: 'drag' as EventName<DragEvent>,
        onDragStart: 'dragstart' as EventName<DragEvent>,
        onDragEnd: 'dragend' as EventName<DragEvent>,
        onDragEnter: 'dragenter' as EventName<DragEvent>,
        onDragLeave: 'dragleave' as EventName<DragEvent>,
        onDragOver: 'dragover' as EventName<DragEvent>,
        onDrop: 'drop' as EventName<DragEvent>,
        onCopy: 'copy' as EventName<ClipboardEvent>,
        onCut: 'cut' as EventName<ClipboardEvent>,
        onPaste: 'paste' as EventName<ClipboardEvent>,
        onAnimationStart: 'animationstart' as EventName<AnimationEvent>,
        onAnimationEnd: 'animationend' as EventName<AnimationEvent>,
        onAnimationIteration: 'animationiteration' as EventName<AnimationEvent>,
        onTransitionEnd: 'transitionend' as EventName<TransitionEvent>,
        onFlintReposition: FlintPopupEvents.REPOSITION as EventName<CustomEvent>,
    },
}) as unknown as React.ForwardRefExoticComponent<FlintPopupProps & React.RefAttributes<FlintPopupElement>>;
