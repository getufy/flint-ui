// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/table/ui-table.ts

import React from 'react';
import type { UiTableCell as UiTableCellElement } from '../../../src/table/ui-table.js';
// Register the custom element
import '../../../src/table/ui-table.js';

export interface UiTableCellProps extends React.HTMLAttributes<HTMLElement> {
    header?: boolean;
    align?: 'left' | 'right' | 'center';
}

export const UiTableCell = React.forwardRef<UiTableCellElement, UiTableCellProps>(
    function UiTableCell(
        { header, align, children, ...htmlProps }: UiTableCellProps,
        ref: React.ForwardedRef<UiTableCellElement>,
    ) {
        const innerRef = React.useRef<UiTableCellElement>(null);
        const elRef = (ref as React.RefObject<UiTableCellElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (header !== undefined) (el as unknown as UiTableCellElement).header = header;
        if (align !== undefined) (el as unknown as UiTableCellElement).align = align;
        }, [header, align]);

        return (
            <ui-table-cell ref={elRef} {...htmlProps}>
                {children}
            </ui-table-cell>
        );
    },
);

UiTableCell.displayName = 'UiTableCell';
