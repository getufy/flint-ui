// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/stack/ui-stack.ts

import React from 'react';
import type { UiStack as UiStackElement } from '../../../src/stack/ui-stack.js';
// Register the custom element
import '../../../src/stack/ui-stack.js';

export interface UiStackProps extends React.HTMLAttributes<HTMLElement> {
    direction?: ResponsiveValue<'row' | 'row-reverse' | 'column' | 'column-reverse'>;
    spacing?: ResponsiveValue<number | string>;
    alignItems?: 'flex-start' | 'center' | 'flex-end' | 'stretch' | 'baseline';
    justifyContent?: 'flex-start' | 'center' | 'flex-end' | 'space-between' | 'space-around' | 'space-evenly';
    useFlexGap?: boolean;
}

export const UiStack = React.forwardRef<UiStackElement, UiStackProps>(
    function UiStack(
        { direction, spacing, alignItems, justifyContent, useFlexGap, children, ...htmlProps }: UiStackProps,
        ref: React.ForwardedRef<UiStackElement>,
    ) {
        const innerRef = React.useRef<UiStackElement>(null);
        const elRef = (ref as React.RefObject<UiStackElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (direction !== undefined) (el as unknown as UiStackElement).direction = direction;
        if (spacing !== undefined) (el as unknown as UiStackElement).spacing = spacing;
        if (alignItems !== undefined) (el as unknown as UiStackElement).alignItems = alignItems;
        if (justifyContent !== undefined) (el as unknown as UiStackElement).justifyContent = justifyContent;
        if (useFlexGap !== undefined) (el as unknown as UiStackElement).useFlexGap = useFlexGap;
        }, [direction, spacing, alignItems, justifyContent, useFlexGap]);

        return (
            <ui-stack ref={elRef} {...htmlProps}>
                {children}
            </ui-stack>
        );
    },
);

UiStack.displayName = 'UiStack';
