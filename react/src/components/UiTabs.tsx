// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/tabs/ui-tabs.ts

import React from 'react';
import type { UiTabs as UiTabsElement } from '../../../src/tabs/ui-tabs.js';
import { UiTabsEvents } from '../events/ui-tabs.js';
// Register the custom element
import '../../../src/tabs/ui-tabs.js';

export interface UiTabsProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    orientation?: 'horizontal' | 'vertical';
    variant?: 'standard' | 'fullWidth' | 'scrollable';
    centered?: boolean;
    scrollButtons?: 'auto' | 'false';
    textColor?: string;
    indicatorColor?: string;
    onUiTabChange?: (event: CustomEvent) => void;
}

export const UiTabs = React.forwardRef<UiTabsElement, UiTabsProps>(
    function UiTabs(
        { value, orientation, variant, centered, scrollButtons, textColor, indicatorColor, onUiTabChange, children, ...htmlProps }: UiTabsProps,
        ref: React.ForwardedRef<UiTabsElement>,
    ) {
        const innerRef = React.useRef<UiTabsElement>(null);
        const elRef = (ref as React.RefObject<UiTabsElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiTabsElement).value = value;
        if (orientation !== undefined) (el as unknown as UiTabsElement).orientation = orientation;
        if (variant !== undefined) (el as unknown as UiTabsElement).variant = variant;
        if (centered !== undefined) (el as unknown as UiTabsElement).centered = centered;
        if (scrollButtons !== undefined) (el as unknown as UiTabsElement).scrollButtons = scrollButtons;
        if (textColor !== undefined) (el as unknown as UiTabsElement).textColor = textColor;
        if (indicatorColor !== undefined) (el as unknown as UiTabsElement).indicatorColor = indicatorColor;
        }, [value, orientation, variant, centered, scrollButtons, textColor, indicatorColor]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiTabChange) return;
            const fn = onUiTabChange as EventListener;
            el.addEventListener(UiTabsEvents.TAB_CHANGE, fn);
            return () => el.removeEventListener(UiTabsEvents.TAB_CHANGE, fn);
        }, [onUiTabChange]);
    
        return (
            <ui-tabs ref={elRef} {...htmlProps}>
                {children}
            </ui-tabs>
        );
    },
);

UiTabs.displayName = 'UiTabs';
