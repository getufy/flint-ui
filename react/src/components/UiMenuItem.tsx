// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/menu/ui-menu.ts

import React from 'react';
import type { UiMenuItem as UiMenuItemElement } from '../../../src/menu/ui-menu.js';
import { UiMenuItemEvents } from '../events/ui-menu-item.js';
// Register the custom element
import '../../../src/menu/ui-menu.js';

export interface UiMenuItemProps extends React.HTMLAttributes<HTMLElement> {
    selected?: boolean;
    disabled?: boolean;
    dense?: boolean;
    divider?: boolean;
    value?: string;
    onUiMenuItemSelect?: (event: CustomEvent) => void;
}

export const UiMenuItem = React.forwardRef<UiMenuItemElement, UiMenuItemProps>(
    function UiMenuItem(
        { selected, disabled, dense, divider, value, onUiMenuItemSelect, children, ...htmlProps }: UiMenuItemProps,
        ref: React.ForwardedRef<UiMenuItemElement>,
    ) {
        const innerRef = React.useRef<UiMenuItemElement>(null);
        const elRef = (ref as React.RefObject<UiMenuItemElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (selected !== undefined) (el as unknown as UiMenuItemElement).selected = selected;
        if (disabled !== undefined) (el as unknown as UiMenuItemElement).disabled = disabled;
        if (dense !== undefined) (el as unknown as UiMenuItemElement).dense = dense;
        if (divider !== undefined) (el as unknown as UiMenuItemElement).divider = divider;
        if (value !== undefined) (el as unknown as UiMenuItemElement).value = value;
        }, [selected, disabled, dense, divider, value]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiMenuItemSelect) return;
            const fn = onUiMenuItemSelect as EventListener;
            el.addEventListener(UiMenuItemEvents.SELECT, fn);
            return () => el.removeEventListener(UiMenuItemEvents.SELECT, fn);
        }, [onUiMenuItemSelect]);
    
        return (
            <ui-menu-item ref={elRef} {...htmlProps}>
                {children}
            </ui-menu-item>
        );
    },
);

UiMenuItem.displayName = 'UiMenuItem';
