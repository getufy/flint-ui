// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/link/ui-link.ts

import React from 'react';
import type { UiLink as UiLinkElement } from '../../../src/link/ui-link.js';
// Register the custom element
import '../../../src/link/ui-link.js';

export interface UiLinkProps extends React.HTMLAttributes<HTMLElement> {
    href?: string;
    target?: '_self' | '_blank' | '_parent' | '_top';
    rel?: string;
    color?: | 'primary' | 'secondary' | 'success' | 'error' | 'warning' | 'info'
        | 'textPrimary' | 'textSecondary' | 'inherit';
    underline?: 'none' | 'hover' | 'always';
    variant?: | 'inherit' | 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6'
        | 'subtitle1' | 'subtitle2' | 'body1' | 'body2' | 'caption' | 'overline';
    disabled?: boolean;
    download?: string;
    label?: string;
}

export const UiLink = React.forwardRef<UiLinkElement, UiLinkProps>(
    function UiLink(
        { href, target, rel, color, underline, variant, disabled, download, label, children, ...htmlProps }: UiLinkProps,
        ref: React.ForwardedRef<UiLinkElement>,
    ) {
        const innerRef = React.useRef<UiLinkElement>(null);
        const elRef = (ref as React.RefObject<UiLinkElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (href !== undefined) (el as unknown as UiLinkElement).href = href;
        if (target !== undefined) (el as unknown as UiLinkElement).target = target;
        if (rel !== undefined) (el as unknown as UiLinkElement).rel = rel;
        if (color !== undefined) (el as unknown as UiLinkElement).color = color;
        if (underline !== undefined) (el as unknown as UiLinkElement).underline = underline;
        if (variant !== undefined) (el as unknown as UiLinkElement).variant = variant;
        if (disabled !== undefined) (el as unknown as UiLinkElement).disabled = disabled;
        if (download !== undefined) (el as unknown as UiLinkElement).download = download;
        if (label !== undefined) (el as unknown as UiLinkElement).label = label;
        }, [href, target, rel, color, underline, variant, disabled, download, label]);

        return (
            <ui-link ref={elRef} {...htmlProps}>
                {children}
            </ui-link>
        );
    },
);

UiLink.displayName = 'UiLink';
