// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/pagination/ui-pagination.ts

import React from 'react';
import type { UiPagination as UiPaginationElement } from '../../../src/pagination/ui-pagination.js';
import { UiPaginationEvents } from '../events/ui-pagination.js';
// Register the custom element
import '../../../src/pagination/ui-pagination.js';

export interface UiPaginationProps extends React.HTMLAttributes<HTMLElement> {
    count?: number;
    page?: number;
    defaultPage?: number;
    label?: string;
    variant?: 'text' | 'outlined';
    shape?: 'circular' | 'rounded' | 'square';
    size?: 'small' | 'medium' | 'large';
    color?: 'primary' | 'secondary' | 'standard';
    showFirstButton?: boolean;
    showLastButton?: boolean;
    hidePrevButton?: boolean;
    hideNextButton?: boolean;
    siblingCount?: number;
    boundaryCount?: number;
    disabled?: boolean;
    onUiPaginationChange?: (event: CustomEvent) => void;
}

export const UiPagination = React.forwardRef<UiPaginationElement, UiPaginationProps>(
    function UiPagination(
        { count, page, defaultPage, label, variant, shape, size, color, showFirstButton, showLastButton, hidePrevButton, hideNextButton, siblingCount, boundaryCount, disabled, onUiPaginationChange, children, ...htmlProps }: UiPaginationProps,
        ref: React.ForwardedRef<UiPaginationElement>,
    ) {
        const innerRef = React.useRef<UiPaginationElement>(null);
        const elRef = (ref as React.RefObject<UiPaginationElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (count !== undefined) (el as unknown as UiPaginationElement).count = count;
        if (page !== undefined) (el as unknown as UiPaginationElement).page = page;
        if (defaultPage !== undefined) (el as unknown as UiPaginationElement).defaultPage = defaultPage;
        if (label !== undefined) (el as unknown as UiPaginationElement).label = label;
        if (variant !== undefined) (el as unknown as UiPaginationElement).variant = variant;
        if (shape !== undefined) (el as unknown as UiPaginationElement).shape = shape;
        if (size !== undefined) (el as unknown as UiPaginationElement).size = size;
        if (color !== undefined) (el as unknown as UiPaginationElement).color = color;
        if (showFirstButton !== undefined) (el as unknown as UiPaginationElement).showFirstButton = showFirstButton;
        if (showLastButton !== undefined) (el as unknown as UiPaginationElement).showLastButton = showLastButton;
        if (hidePrevButton !== undefined) (el as unknown as UiPaginationElement).hidePrevButton = hidePrevButton;
        if (hideNextButton !== undefined) (el as unknown as UiPaginationElement).hideNextButton = hideNextButton;
        if (siblingCount !== undefined) (el as unknown as UiPaginationElement).siblingCount = siblingCount;
        if (boundaryCount !== undefined) (el as unknown as UiPaginationElement).boundaryCount = boundaryCount;
        if (disabled !== undefined) (el as unknown as UiPaginationElement).disabled = disabled;
        }, [count, page, defaultPage, label, variant, shape, size, color, showFirstButton, showLastButton, hidePrevButton, hideNextButton, siblingCount, boundaryCount, disabled]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiPaginationChange) return;
            const fn = onUiPaginationChange as EventListener;
            el.addEventListener(UiPaginationEvents.CHANGE, fn);
            return () => el.removeEventListener(UiPaginationEvents.CHANGE, fn);
        }, [onUiPaginationChange]);
    
        return (
            <ui-pagination ref={elRef} {...htmlProps}>
                {children}
            </ui-pagination>
        );
    },
);

UiPagination.displayName = 'UiPagination';
