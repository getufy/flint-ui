// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/container/ui-container.ts

import React from 'react';
import type { UiContainer as UiContainerElement } from '../../../src/container/ui-container.js';
// Register the custom element
import '../../../src/container/ui-container.js';

export interface UiContainerProps extends React.HTMLAttributes<HTMLElement> {
    maxWidth?: ContainerMaxWidth;
    disableGutters?: boolean;
    fixed?: boolean;
}

export const UiContainer = React.forwardRef<UiContainerElement, UiContainerProps>(
    function UiContainer(
        { maxWidth, disableGutters, fixed, children, ...htmlProps }: UiContainerProps,
        ref: React.ForwardedRef<UiContainerElement>,
    ) {
        const innerRef = React.useRef<UiContainerElement>(null);
        const elRef = (ref as React.RefObject<UiContainerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (maxWidth !== undefined) (el as unknown as UiContainerElement).maxWidth = maxWidth;
        if (disableGutters !== undefined) (el as unknown as UiContainerElement).disableGutters = disableGutters;
        if (fixed !== undefined) (el as unknown as UiContainerElement).fixed = fixed;
        }, [maxWidth, disableGutters, fixed]);

        return (
            <ui-container ref={elRef} {...htmlProps}>
                {children}
            </ui-container>
        );
    },
);

UiContainer.displayName = 'UiContainer';
