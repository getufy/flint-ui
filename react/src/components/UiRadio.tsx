// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/radio/ui-radio.ts

import React from 'react';
import type { UiRadio as UiRadioElement } from '../../../src/radio/ui-radio.js';
import { UiRadioEvents } from '../events/ui-radio.js';
// Register the custom element
import '../../../src/radio/ui-radio.js';

export interface UiRadioProps extends React.HTMLAttributes<HTMLElement> {
    checked?: boolean;
    disabled?: boolean;
    required?: boolean;
    name?: string;
    value?: string;
    label?: string;
    size?: RadioSize;
    onUiRadioSelect?: (event: CustomEvent) => void;
}

export const UiRadio = React.forwardRef<UiRadioElement, UiRadioProps>(
    function UiRadio(
        { checked, disabled, required, name, value, label, size, onUiRadioSelect, children, ...htmlProps }: UiRadioProps,
        ref: React.ForwardedRef<UiRadioElement>,
    ) {
        const innerRef = React.useRef<UiRadioElement>(null);
        const elRef = (ref as React.RefObject<UiRadioElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (checked !== undefined) (el as unknown as UiRadioElement).checked = checked;
        if (disabled !== undefined) (el as unknown as UiRadioElement).disabled = disabled;
        if (required !== undefined) (el as unknown as UiRadioElement).required = required;
        if (name !== undefined) (el as unknown as UiRadioElement).name = name;
        if (value !== undefined) (el as unknown as UiRadioElement).value = value;
        if (label !== undefined) (el as unknown as UiRadioElement).label = label;
        if (size !== undefined) (el as unknown as UiRadioElement).size = size;
        }, [checked, disabled, required, name, value, label, size]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiRadioSelect) return;
            const fn = onUiRadioSelect as EventListener;
            el.addEventListener(UiRadioEvents.SELECT, fn);
            return () => el.removeEventListener(UiRadioEvents.SELECT, fn);
        }, [onUiRadioSelect]);
    
        return (
            <ui-radio ref={elRef} {...htmlProps}>
                {children}
            </ui-radio>
        );
    },
);

UiRadio.displayName = 'UiRadio';
