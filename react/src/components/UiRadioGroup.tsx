// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/radio/ui-radio.ts

import React from 'react';
import type { UiRadioGroup as UiRadioGroupElement } from '../../../src/radio/ui-radio.js';
import { UiRadioGroupEvents } from '../events/ui-radio-group.js';
// Register the custom element
import '../../../src/radio/ui-radio.js';

export interface UiRadioGroupProps extends React.HTMLAttributes<HTMLElement> {
    label?: string;
    name?: string;
    value?: string;
    defaultValue?: string;
    disabled?: boolean;
    required?: boolean;
    orientation?: RadioOrientation;
    size?: RadioSize;
    onUiRadioGroupChange?: (event: CustomEvent) => void;
}

export const UiRadioGroup = React.forwardRef<UiRadioGroupElement, UiRadioGroupProps>(
    function UiRadioGroup(
        { label, name, value, defaultValue, disabled, required, orientation, size, onUiRadioGroupChange, children, ...htmlProps }: UiRadioGroupProps,
        ref: React.ForwardedRef<UiRadioGroupElement>,
    ) {
        const innerRef = React.useRef<UiRadioGroupElement>(null);
        const elRef = (ref as React.RefObject<UiRadioGroupElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (label !== undefined) (el as unknown as UiRadioGroupElement).label = label;
        if (name !== undefined) (el as unknown as UiRadioGroupElement).name = name;
        if (value !== undefined) (el as unknown as UiRadioGroupElement).value = value;
        if (defaultValue !== undefined) (el as unknown as UiRadioGroupElement).defaultValue = defaultValue;
        if (disabled !== undefined) (el as unknown as UiRadioGroupElement).disabled = disabled;
        if (required !== undefined) (el as unknown as UiRadioGroupElement).required = required;
        if (orientation !== undefined) (el as unknown as UiRadioGroupElement).orientation = orientation;
        if (size !== undefined) (el as unknown as UiRadioGroupElement).size = size;
        }, [label, name, value, defaultValue, disabled, required, orientation, size]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiRadioGroupChange) return;
            const fn = onUiRadioGroupChange as EventListener;
            el.addEventListener(UiRadioGroupEvents.CHANGE, fn);
            return () => el.removeEventListener(UiRadioGroupEvents.CHANGE, fn);
        }, [onUiRadioGroupChange]);
    
        return (
            <ui-radio-group ref={elRef} {...htmlProps}>
                {children}
            </ui-radio-group>
        );
    },
);

UiRadioGroup.displayName = 'UiRadioGroup';
