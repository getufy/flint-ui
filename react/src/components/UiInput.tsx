// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/input/ui-input.ts

import React from 'react';
import type { UiInput as UiInputElement } from '../../../src/input/ui-input.js';
import { UiInputEvents } from '../events/ui-input.js';
// Register the custom element
import '../../../src/input/ui-input.js';

export interface UiInputProps extends React.HTMLAttributes<HTMLElement> {
    label?: string;
    value?: string;
    type?: string;
    placeholder?: string;
    helpText?: string;
    error?: boolean;
    errorMessage?: string;
    disabled?: boolean;
    required?: boolean;
    readonly?: boolean;
    name?: string;
    autocomplete?: string;
    size?: 'sm' | 'default' | 'lg';
    onUiInputInput?: (event: CustomEvent) => void;
    onUiInputChange?: (event: CustomEvent) => void;
}

export const UiInput = React.forwardRef<UiInputElement, UiInputProps>(
    function UiInput(
        { label, value, type, placeholder, helpText, error, errorMessage, disabled, required, readonly, name, autocomplete, size, onUiInputInput, onUiInputChange, children, ...htmlProps }: UiInputProps,
        ref: React.ForwardedRef<UiInputElement>,
    ) {
        const innerRef = React.useRef<UiInputElement>(null);
        const elRef = (ref as React.RefObject<UiInputElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (label !== undefined) (el as unknown as UiInputElement).label = label;
        if (value !== undefined) (el as unknown as UiInputElement).value = value;
        if (type !== undefined) (el as unknown as UiInputElement).type = type;
        if (placeholder !== undefined) (el as unknown as UiInputElement).placeholder = placeholder;
        if (helpText !== undefined) (el as unknown as UiInputElement).helpText = helpText;
        if (error !== undefined) (el as unknown as UiInputElement).error = error;
        if (errorMessage !== undefined) (el as unknown as UiInputElement).errorMessage = errorMessage;
        if (disabled !== undefined) (el as unknown as UiInputElement).disabled = disabled;
        if (required !== undefined) (el as unknown as UiInputElement).required = required;
        if (readonly !== undefined) (el as unknown as UiInputElement).readonly = readonly;
        if (name !== undefined) (el as unknown as UiInputElement).name = name;
        if (autocomplete !== undefined) (el as unknown as UiInputElement).autocomplete = autocomplete;
        if (size !== undefined) (el as unknown as UiInputElement).size = size;
        }, [label, value, type, placeholder, helpText, error, errorMessage, disabled, required, readonly, name, autocomplete, size]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiInputInput) return;
            const fn = onUiInputInput as EventListener;
            el.addEventListener(UiInputEvents.INPUT, fn);
            return () => el.removeEventListener(UiInputEvents.INPUT, fn);
        }, [onUiInputInput]);
    
        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiInputChange) return;
            const fn = onUiInputChange as EventListener;
            el.addEventListener(UiInputEvents.CHANGE, fn);
            return () => el.removeEventListener(UiInputEvents.CHANGE, fn);
        }, [onUiInputChange]);
    
        return (
            <ui-input ref={elRef} {...htmlProps}>
                {children}
            </ui-input>
        );
    },
);

UiInput.displayName = 'UiInput';
