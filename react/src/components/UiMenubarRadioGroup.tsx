// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/menubar/ui-menubar.ts

import React from 'react';
import type { UiMenubarRadioGroup as UiMenubarRadioGroupElement } from '../../../src/menubar/ui-menubar.js';
import { UiMenubarRadioGroupEvents } from '../events/ui-menubar-radio-group.js';
// Register the custom element
import '../../../src/menubar/ui-menubar.js';

export interface UiMenubarRadioGroupProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    onUiMenubarRadioChange?: (event: CustomEvent) => void;
}

export const UiMenubarRadioGroup = React.forwardRef<UiMenubarRadioGroupElement, UiMenubarRadioGroupProps>(
    function UiMenubarRadioGroup(
        { value, onUiMenubarRadioChange, children, ...htmlProps }: UiMenubarRadioGroupProps,
        ref: React.ForwardedRef<UiMenubarRadioGroupElement>,
    ) {
        const innerRef = React.useRef<UiMenubarRadioGroupElement>(null);
        const elRef = (ref as React.RefObject<UiMenubarRadioGroupElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiMenubarRadioGroupElement).value = value;
        }, [value]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiMenubarRadioChange) return;
            const fn = onUiMenubarRadioChange as EventListener;
            el.addEventListener(UiMenubarRadioGroupEvents.MENUBAR_RADIO_CHANGE, fn);
            return () => el.removeEventListener(UiMenubarRadioGroupEvents.MENUBAR_RADIO_CHANGE, fn);
        }, [onUiMenubarRadioChange]);
    
        return (
            <ui-menubar-radio-group ref={elRef} {...htmlProps}>
                {children}
            </ui-menubar-radio-group>
        );
    },
);

UiMenubarRadioGroup.displayName = 'UiMenubarRadioGroup';
