// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/alert/ui-alert.ts

import React from 'react';
import type { UiAlert as UiAlertElement } from '../../../src/alert/ui-alert.js';
import { UiAlertEvents } from '../events/ui-alert.js';
// Register the custom element
import '../../../src/alert/ui-alert.js';

export interface UiAlertProps extends React.HTMLAttributes<HTMLElement> {
    severity?: 'info' | 'success' | 'warning' | 'error';
    title?: string;
    dismissible?: boolean;
    onUiAlertClose?: (event: CustomEvent) => void;
}

export const UiAlert = React.forwardRef<UiAlertElement, UiAlertProps>(
    function UiAlert(
        { severity, title, dismissible, onUiAlertClose, children, ...htmlProps }: UiAlertProps,
        ref: React.ForwardedRef<UiAlertElement>,
    ) {
        const innerRef = React.useRef<UiAlertElement>(null);
        const elRef = (ref as React.RefObject<UiAlertElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (severity !== undefined) (el as unknown as UiAlertElement).severity = severity;
        if (title !== undefined) (el as unknown as UiAlertElement).title = title;
        if (dismissible !== undefined) (el as unknown as UiAlertElement).dismissible = dismissible;
        }, [severity, title, dismissible]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiAlertClose) return;
            const fn = onUiAlertClose as EventListener;
            el.addEventListener(UiAlertEvents.CLOSE, fn);
            return () => el.removeEventListener(UiAlertEvents.CLOSE, fn);
        }, [onUiAlertClose]);
    
        return (
            <ui-alert ref={elRef} {...htmlProps}>
                {children}
            </ui-alert>
        );
    },
);

UiAlert.displayName = 'UiAlert';
