// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/drawer/ui-drawer.ts

import React from 'react';
import type { UiDrawer as UiDrawerElement } from '../../../src/drawer/ui-drawer.js';
import { UiDrawerEvents } from '../events/ui-drawer.js';
// Register the custom element
import '../../../src/drawer/ui-drawer.js';

export interface UiDrawerProps extends React.HTMLAttributes<HTMLElement> {
    open?: boolean;
    anchor?: 'left' | 'right' | 'top' | 'bottom';
    variant?: 'temporary' | 'persistent' | 'mini';
    edge?: boolean;
    container?: boolean;
    label?: string;
    onUiDrawerClose?: (event: CustomEvent) => void;
}

export const UiDrawer = React.forwardRef<UiDrawerElement, UiDrawerProps>(
    function UiDrawer(
        { open, anchor, variant, edge, container, label, onUiDrawerClose, children, ...htmlProps }: UiDrawerProps,
        ref: React.ForwardedRef<UiDrawerElement>,
    ) {
        const innerRef = React.useRef<UiDrawerElement>(null);
        const elRef = (ref as React.RefObject<UiDrawerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (open !== undefined) (el as unknown as UiDrawerElement).open = open;
        if (anchor !== undefined) (el as unknown as UiDrawerElement).anchor = anchor;
        if (variant !== undefined) (el as unknown as UiDrawerElement).variant = variant;
        if (edge !== undefined) (el as unknown as UiDrawerElement).edge = edge;
        if (container !== undefined) (el as unknown as UiDrawerElement).container = container;
        if (label !== undefined) (el as unknown as UiDrawerElement).label = label;
        }, [open, anchor, variant, edge, container, label]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiDrawerClose) return;
            const fn = onUiDrawerClose as EventListener;
            el.addEventListener(UiDrawerEvents.CLOSE, fn);
            return () => el.removeEventListener(UiDrawerEvents.CLOSE, fn);
        }, [onUiDrawerClose]);
    
        return (
            <ui-drawer ref={elRef} {...htmlProps}>
                {children}
            </ui-drawer>
        );
    },
);

UiDrawer.displayName = 'UiDrawer';
