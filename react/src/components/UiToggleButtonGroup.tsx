// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/button/ui-toggle-button-group.ts

import React from 'react';
import type { UiToggleButtonGroup as UiToggleButtonGroupElement } from '../../../src/button/ui-toggle-button-group.js';
import { UiToggleButtonGroupEvents } from '../events/ui-toggle-button-group.js';
// Register the custom element
import '../../../src/button/ui-toggle-button-group.js';

export interface UiToggleButtonGroupProps extends React.HTMLAttributes<HTMLElement> {
    value?: string | string[];
    exclusive?: boolean;
    onChange?: (event: CustomEvent) => void;
}

export const UiToggleButtonGroup = React.forwardRef<UiToggleButtonGroupElement, UiToggleButtonGroupProps>(
    function UiToggleButtonGroup(
        { value, exclusive, onChange, children, ...htmlProps }: UiToggleButtonGroupProps,
        ref: React.ForwardedRef<UiToggleButtonGroupElement>,
    ) {
        const innerRef = React.useRef<UiToggleButtonGroupElement>(null);
        const elRef = (ref as React.RefObject<UiToggleButtonGroupElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiToggleButtonGroupElement).value = value;
        if (exclusive !== undefined) (el as unknown as UiToggleButtonGroupElement).exclusive = exclusive;
        }, [value, exclusive]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiToggleButtonGroupEvents.CHANGE, fn);
            return () => el.removeEventListener(UiToggleButtonGroupEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-toggle-button-group ref={elRef} {...htmlProps}>
                {children}
            </ui-toggle-button-group>
        );
    },
);

UiToggleButtonGroup.displayName = 'UiToggleButtonGroup';
