// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/table/ui-table.ts

import React from 'react';
import type { UiTableContainer as UiTableContainerElement } from '../../../src/table/ui-table.js';
// Register the custom element
import '../../../src/table/ui-table.js';

export interface UiTableContainerProps extends React.HTMLAttributes<HTMLElement> {
    shadow?: boolean;
}

export const UiTableContainer = React.forwardRef<UiTableContainerElement, UiTableContainerProps>(
    function UiTableContainer(
        { shadow, children, ...htmlProps }: UiTableContainerProps,
        ref: React.ForwardedRef<UiTableContainerElement>,
    ) {
        const innerRef = React.useRef<UiTableContainerElement>(null);
        const elRef = (ref as React.RefObject<UiTableContainerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (shadow !== undefined) (el as unknown as UiTableContainerElement).shadow = shadow;
        }, [shadow]);

        return (
            <ui-table-container ref={elRef} {...htmlProps}>
                {children}
            </ui-table-container>
        );
    },
);

UiTableContainer.displayName = 'UiTableContainer';
