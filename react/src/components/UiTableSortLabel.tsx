// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/table/ui-table-sort-label.ts

import React from 'react';
import type { UiTableSortLabel as UiTableSortLabelElement } from '../../../src/table/ui-table-sort-label.js';
// Register the custom element
import '../../../src/table/ui-table-sort-label.js';

export interface UiTableSortLabelProps extends React.HTMLAttributes<HTMLElement> {
    active?: boolean;
    direction?: 'asc' | 'desc';
}

export const UiTableSortLabel = React.forwardRef<UiTableSortLabelElement, UiTableSortLabelProps>(
    function UiTableSortLabel(
        { active, direction, children, ...htmlProps }: UiTableSortLabelProps,
        ref: React.ForwardedRef<UiTableSortLabelElement>,
    ) {
        const innerRef = React.useRef<UiTableSortLabelElement>(null);
        const elRef = (ref as React.RefObject<UiTableSortLabelElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (active !== undefined) (el as unknown as UiTableSortLabelElement).active = active;
        if (direction !== undefined) (el as unknown as UiTableSortLabelElement).direction = direction;
        }, [active, direction]);

        return (
            <ui-table-sort-label ref={elRef} {...htmlProps}>
                {children}
            </ui-table-sort-label>
        );
    },
);

UiTableSortLabel.displayName = 'UiTableSortLabel';
