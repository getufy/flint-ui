// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/list/ui-list.ts

import React from 'react';
import type { UiList as UiListElement } from '../../../src/list/ui-list.js';
// Register the custom element
import '../../../src/list/ui-list.js';

export interface UiListProps extends React.HTMLAttributes<HTMLElement> {
    disablePadding?: boolean;
    dense?: boolean;
}

export const UiList = React.forwardRef<UiListElement, UiListProps>(
    function UiList(
        { disablePadding, dense, children, ...htmlProps }: UiListProps,
        ref: React.ForwardedRef<UiListElement>,
    ) {
        const innerRef = React.useRef<UiListElement>(null);
        const elRef = (ref as React.RefObject<UiListElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (disablePadding !== undefined) (el as unknown as UiListElement).disablePadding = disablePadding;
        if (dense !== undefined) (el as unknown as UiListElement).dense = dense;
        }, [disablePadding, dense]);

        return (
            <ui-list ref={elRef} {...htmlProps}>
                {children}
            </ui-list>
        );
    },
);

UiList.displayName = 'UiList';
