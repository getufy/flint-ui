// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/card/ui-card-header.ts

import React from 'react';
import type { UiCardHeader as UiCardHeaderElement } from '../../../src/card/ui-card-header.js';
// Register the custom element
import '../../../src/card/ui-card-header.js';

export interface UiCardHeaderProps extends React.HTMLAttributes<HTMLElement> {
    title?: string;
    subtitle?: string;
}

export const UiCardHeader = React.forwardRef<UiCardHeaderElement, UiCardHeaderProps>(
    function UiCardHeader(
        { title, subtitle, children, ...htmlProps }: UiCardHeaderProps,
        ref: React.ForwardedRef<UiCardHeaderElement>,
    ) {
        const innerRef = React.useRef<UiCardHeaderElement>(null);
        const elRef = (ref as React.RefObject<UiCardHeaderElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (title !== undefined) (el as unknown as UiCardHeaderElement).title = title;
        if (subtitle !== undefined) (el as unknown as UiCardHeaderElement).subtitle = subtitle;
        }, [title, subtitle]);

        return (
            <ui-card-header ref={elRef} {...htmlProps}>
                {children}
            </ui-card-header>
        );
    },
);

UiCardHeader.displayName = 'UiCardHeader';
