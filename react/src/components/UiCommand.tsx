// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/command/ui-command.ts

import React from 'react';
import type { UiCommand as UiCommandElement } from '../../../src/command/ui-command.js';
import { UiCommandEvents } from '../events/ui-command.js';
// Register the custom element
import '../../../src/command/ui-command.js';

export interface UiCommandProps extends React.HTMLAttributes<HTMLElement> {
    onUiCommandItemSelect?: (event: CustomEvent) => void;
}

export const UiCommand = React.forwardRef<UiCommandElement, UiCommandProps>(
    function UiCommand(
        { onUiCommandItemSelect, children, ...htmlProps }: UiCommandProps,
        ref: React.ForwardedRef<UiCommandElement>,
    ) {
        const innerRef = React.useRef<UiCommandElement>(null);
        const elRef = (ref as React.RefObject<UiCommandElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiCommandItemSelect) return;
            const fn = onUiCommandItemSelect as EventListener;
            el.addEventListener(UiCommandEvents.ITEM_SELECT, fn);
            return () => el.removeEventListener(UiCommandEvents.ITEM_SELECT, fn);
        }, [onUiCommandItemSelect]);
    
        return (
            <ui-command ref={elRef} {...htmlProps}>
                {children}
            </ui-command>
        );
    },
);

UiCommand.displayName = 'UiCommand';
