// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/input-otp/ui-input-otp.ts

import React from 'react';
import type { UiInputOtp as UiInputOtpElement } from '../../../src/input-otp/ui-input-otp.js';
import { UiInputOtpEvents } from '../events/ui-input-otp.js';
// Register the custom element
import '../../../src/input-otp/ui-input-otp.js';

export interface UiInputOtpProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    defaultValue?: string;
    maxLength?: number;
    pattern?: string;
    disabled?: boolean;
    onUiOtpChange?: (event: CustomEvent) => void;
    onUiOtpComplete?: (event: CustomEvent) => void;
}

export const UiInputOtp = React.forwardRef<UiInputOtpElement, UiInputOtpProps>(
    function UiInputOtp(
        { value, defaultValue, maxLength, pattern, disabled, onUiOtpChange, onUiOtpComplete, children, ...htmlProps }: UiInputOtpProps,
        ref: React.ForwardedRef<UiInputOtpElement>,
    ) {
        const innerRef = React.useRef<UiInputOtpElement>(null);
        const elRef = (ref as React.RefObject<UiInputOtpElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiInputOtpElement).value = value;
        if (defaultValue !== undefined) (el as unknown as UiInputOtpElement).defaultValue = defaultValue;
        if (maxLength !== undefined) (el as unknown as UiInputOtpElement).maxLength = maxLength;
        if (pattern !== undefined) (el as unknown as UiInputOtpElement).pattern = pattern;
        if (disabled !== undefined) (el as unknown as UiInputOtpElement).disabled = disabled;
        }, [value, defaultValue, maxLength, pattern, disabled]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiOtpChange) return;
            const fn = onUiOtpChange as EventListener;
            el.addEventListener(UiInputOtpEvents.OTP_CHANGE, fn);
            return () => el.removeEventListener(UiInputOtpEvents.OTP_CHANGE, fn);
        }, [onUiOtpChange]);
    
        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiOtpComplete) return;
            const fn = onUiOtpComplete as EventListener;
            el.addEventListener(UiInputOtpEvents.OTP_COMPLETE, fn);
            return () => el.removeEventListener(UiInputOtpEvents.OTP_COMPLETE, fn);
        }, [onUiOtpComplete]);
    
        return (
            <ui-input-otp ref={elRef} {...htmlProps}>
                {children}
            </ui-input-otp>
        );
    },
);

UiInputOtp.displayName = 'UiInputOtp';
