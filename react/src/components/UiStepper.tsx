// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/stepper/ui-stepper.ts

import React from 'react';
import type { UiStepper as UiStepperElement } from '../../../src/stepper/ui-stepper.js';
import { UiStepperEvents } from '../events/ui-stepper.js';
// Register the custom element
import '../../../src/stepper/ui-stepper.js';

export interface UiStepperProps extends React.HTMLAttributes<HTMLElement> {
    activeStep?: number;
    orientation?: 'horizontal' | 'vertical';
    alternativeLabel?: boolean;
    nonLinear?: boolean;
    label?: string;
    onUiStepChange?: (event: CustomEvent) => void;
}

export const UiStepper = React.forwardRef<UiStepperElement, UiStepperProps>(
    function UiStepper(
        { activeStep, orientation, alternativeLabel, nonLinear, label, onUiStepChange, children, ...htmlProps }: UiStepperProps,
        ref: React.ForwardedRef<UiStepperElement>,
    ) {
        const innerRef = React.useRef<UiStepperElement>(null);
        const elRef = (ref as React.RefObject<UiStepperElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (activeStep !== undefined) (el as unknown as UiStepperElement).activeStep = activeStep;
        if (orientation !== undefined) (el as unknown as UiStepperElement).orientation = orientation;
        if (alternativeLabel !== undefined) (el as unknown as UiStepperElement).alternativeLabel = alternativeLabel;
        if (nonLinear !== undefined) (el as unknown as UiStepperElement).nonLinear = nonLinear;
        if (label !== undefined) (el as unknown as UiStepperElement).label = label;
        }, [activeStep, orientation, alternativeLabel, nonLinear, label]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiStepChange) return;
            const fn = onUiStepChange as EventListener;
            el.addEventListener(UiStepperEvents.STEP_CHANGE, fn);
            return () => el.removeEventListener(UiStepperEvents.STEP_CHANGE, fn);
        }, [onUiStepChange]);
    
        return (
            <ui-stepper ref={elRef} {...htmlProps}>
                {children}
            </ui-stepper>
        );
    },
);

UiStepper.displayName = 'UiStepper';
