// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/ui-range-slider/ui-range-slider.ts

import React from 'react';
import type { UiRangeSlider as UiRangeSliderElement } from '../../../src/ui-range-slider/ui-range-slider.js';
import { UiRangeSliderEvents } from '../events/ui-range-slider.js';
// Register the custom element
import '../../../src/ui-range-slider/ui-range-slider.js';

export interface UiRangeSliderProps extends React.HTMLAttributes<HTMLElement> {
    value?: [number, number];
    min?: number;
    max?: number;
    step?: number;
    disabled?: boolean;
    label?: string;
    showValue?: boolean;
    onChange?: (event: CustomEvent) => void;
}

export const UiRangeSlider = React.forwardRef<UiRangeSliderElement, UiRangeSliderProps>(
    function UiRangeSlider(
        { value, min, max, step, disabled, label, showValue, onChange, children, ...htmlProps }: UiRangeSliderProps,
        ref: React.ForwardedRef<UiRangeSliderElement>,
    ) {
        const innerRef = React.useRef<UiRangeSliderElement>(null);
        const elRef = (ref as React.RefObject<UiRangeSliderElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiRangeSliderElement).value = value;
        if (min !== undefined) (el as unknown as UiRangeSliderElement).min = min;
        if (max !== undefined) (el as unknown as UiRangeSliderElement).max = max;
        if (step !== undefined) (el as unknown as UiRangeSliderElement).step = step;
        if (disabled !== undefined) (el as unknown as UiRangeSliderElement).disabled = disabled;
        if (label !== undefined) (el as unknown as UiRangeSliderElement).label = label;
        if (showValue !== undefined) (el as unknown as UiRangeSliderElement).showValue = showValue;
        }, [value, min, max, step, disabled, label, showValue]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiRangeSliderEvents.CHANGE, fn);
            return () => el.removeEventListener(UiRangeSliderEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-range-slider ref={elRef} {...htmlProps}>
                {children}
            </ui-range-slider>
        );
    },
);

UiRangeSlider.displayName = 'UiRangeSlider';
