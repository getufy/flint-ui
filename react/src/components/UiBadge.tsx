// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/badge/ui-badge.ts

import React from 'react';
import type { UiBadge as UiBadgeElement } from '../../../src/badge/ui-badge.js';
// Register the custom element
import '../../../src/badge/ui-badge.js';

export interface UiBadgeProps extends React.HTMLAttributes<HTMLElement> {
    content?: string;
    dot?: boolean;
    invisible?: boolean;
    variant?: 'primary' | 'secondary' | 'error' | 'success' | 'warning';
    max?: number;
}

export const UiBadge = React.forwardRef<UiBadgeElement, UiBadgeProps>(
    function UiBadge(
        { content, dot, invisible, variant, max, children, ...htmlProps }: UiBadgeProps,
        ref: React.ForwardedRef<UiBadgeElement>,
    ) {
        const innerRef = React.useRef<UiBadgeElement>(null);
        const elRef = (ref as React.RefObject<UiBadgeElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (content !== undefined) (el as unknown as UiBadgeElement).content = content;
        if (dot !== undefined) (el as unknown as UiBadgeElement).dot = dot;
        if (invisible !== undefined) (el as unknown as UiBadgeElement).invisible = invisible;
        if (variant !== undefined) (el as unknown as UiBadgeElement).variant = variant;
        if (max !== undefined) (el as unknown as UiBadgeElement).max = max;
        }, [content, dot, invisible, variant, max]);

        return (
            <ui-badge ref={elRef} {...htmlProps}>
                {children}
            </ui-badge>
        );
    },
);

UiBadge.displayName = 'UiBadge';
