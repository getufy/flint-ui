// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/menubar/ui-menubar.ts

import React from 'react';
import type { UiMenubarMenu as UiMenubarMenuElement } from '../../../src/menubar/ui-menubar.js';
// Register the custom element
import '../../../src/menubar/ui-menubar.js';

export interface UiMenubarMenuProps extends React.HTMLAttributes<HTMLElement> {
    disabled?: boolean;
}

export const UiMenubarMenu = React.forwardRef<UiMenubarMenuElement, UiMenubarMenuProps>(
    function UiMenubarMenu(
        { disabled, children, ...htmlProps }: UiMenubarMenuProps,
        ref: React.ForwardedRef<UiMenubarMenuElement>,
    ) {
        const innerRef = React.useRef<UiMenubarMenuElement>(null);
        const elRef = (ref as React.RefObject<UiMenubarMenuElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (disabled !== undefined) (el as unknown as UiMenubarMenuElement).disabled = disabled;
        }, [disabled]);

        return (
            <ui-menubar-menu ref={elRef} {...htmlProps}>
                {children}
            </ui-menubar-menu>
        );
    },
);

UiMenubarMenu.displayName = 'UiMenubarMenu';
