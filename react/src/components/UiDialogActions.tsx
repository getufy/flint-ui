// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/dialog/ui-dialog.ts

import React from 'react';
import type { UiDialogActions as UiDialogActionsElement } from '../../../src/dialog/ui-dialog.js';
// Register the custom element
import '../../../src/dialog/ui-dialog.js';

export interface UiDialogActionsProps extends React.HTMLAttributes<HTMLElement> {
    align?: 'start' | 'center' | 'end' | 'space-between';
}

export const UiDialogActions = React.forwardRef<UiDialogActionsElement, UiDialogActionsProps>(
    function UiDialogActions(
        { align, children, ...htmlProps }: UiDialogActionsProps,
        ref: React.ForwardedRef<UiDialogActionsElement>,
    ) {
        const innerRef = React.useRef<UiDialogActionsElement>(null);
        const elRef = (ref as React.RefObject<UiDialogActionsElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (align !== undefined) (el as unknown as UiDialogActionsElement).align = align;
        }, [align]);

        return (
            <ui-dialog-actions ref={elRef} {...htmlProps}>
                {children}
            </ui-dialog-actions>
        );
    },
);

UiDialogActions.displayName = 'UiDialogActions';
