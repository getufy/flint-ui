// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/tabs/ui-tabs.ts

import React from 'react';
import type { UiTab as UiTabElement } from '../../../src/tabs/ui-tabs.js';
import { UiTabEvents } from '../events/ui-tab.js';
// Register the custom element
import '../../../src/tabs/ui-tabs.js';

export interface UiTabProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    disabled?: boolean;
    selected?: boolean;
    iconPosition?: 'top' | 'bottom' | 'start' | 'end';
    href?: string;
    fullWidth?: boolean;
    onUiTabClick?: (event: CustomEvent) => void;
}

export const UiTab = React.forwardRef<UiTabElement, UiTabProps>(
    function UiTab(
        { value, disabled, selected, iconPosition, href, fullWidth, onUiTabClick, children, ...htmlProps }: UiTabProps,
        ref: React.ForwardedRef<UiTabElement>,
    ) {
        const innerRef = React.useRef<UiTabElement>(null);
        const elRef = (ref as React.RefObject<UiTabElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiTabElement).value = value;
        if (disabled !== undefined) (el as unknown as UiTabElement).disabled = disabled;
        if (selected !== undefined) (el as unknown as UiTabElement).selected = selected;
        if (iconPosition !== undefined) (el as unknown as UiTabElement).iconPosition = iconPosition;
        if (href !== undefined) (el as unknown as UiTabElement).href = href;
        if (fullWidth !== undefined) (el as unknown as UiTabElement).fullWidth = fullWidth;
        }, [value, disabled, selected, iconPosition, href, fullWidth]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiTabClick) return;
            const fn = onUiTabClick as EventListener;
            el.addEventListener(UiTabEvents.CLICK, fn);
            return () => el.removeEventListener(UiTabEvents.CLICK, fn);
        }, [onUiTabClick]);
    
        return (
            <ui-tab ref={elRef} {...htmlProps}>
                {children}
            </ui-tab>
        );
    },
);

UiTab.displayName = 'UiTab';
