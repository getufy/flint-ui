// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/menubar/ui-menubar.ts

import React from 'react';
import type { UiMenubarSubTrigger as UiMenubarSubTriggerElement } from '../../../src/menubar/ui-menubar.js';
// Register the custom element
import '../../../src/menubar/ui-menubar.js';

export interface UiMenubarSubTriggerProps extends React.HTMLAttributes<HTMLElement> {
    highlighted?: boolean;
    disabled?: boolean;
    inset?: boolean;
    expanded?: boolean;
}

export const UiMenubarSubTrigger = React.forwardRef<UiMenubarSubTriggerElement, UiMenubarSubTriggerProps>(
    function UiMenubarSubTrigger(
        { highlighted, disabled, inset, expanded, children, ...htmlProps }: UiMenubarSubTriggerProps,
        ref: React.ForwardedRef<UiMenubarSubTriggerElement>,
    ) {
        const innerRef = React.useRef<UiMenubarSubTriggerElement>(null);
        const elRef = (ref as React.RefObject<UiMenubarSubTriggerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (highlighted !== undefined) (el as unknown as UiMenubarSubTriggerElement).highlighted = highlighted;
        if (disabled !== undefined) (el as unknown as UiMenubarSubTriggerElement).disabled = disabled;
        if (inset !== undefined) (el as unknown as UiMenubarSubTriggerElement).inset = inset;
        if (expanded !== undefined) (el as unknown as UiMenubarSubTriggerElement).expanded = expanded;
        }, [highlighted, disabled, inset, expanded]);

        return (
            <ui-menubar-sub-trigger ref={elRef} {...htmlProps}>
                {children}
            </ui-menubar-sub-trigger>
        );
    },
);

UiMenubarSubTrigger.displayName = 'UiMenubarSubTrigger';
