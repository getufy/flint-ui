// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/carousel/ui-carousel.ts

import React from 'react';
import type { UiCarouselNext as UiCarouselNextElement } from '../../../src/carousel/ui-carousel.js';
// Register the custom element
import '../../../src/carousel/ui-carousel.js';

export interface UiCarouselNextProps extends React.HTMLAttributes<HTMLElement> {
    disabled?: boolean;
    orientation?: 'horizontal' | 'vertical';
}

export const UiCarouselNext = React.forwardRef<UiCarouselNextElement, UiCarouselNextProps>(
    function UiCarouselNext(
        { disabled, orientation, children, ...htmlProps }: UiCarouselNextProps,
        ref: React.ForwardedRef<UiCarouselNextElement>,
    ) {
        const innerRef = React.useRef<UiCarouselNextElement>(null);
        const elRef = (ref as React.RefObject<UiCarouselNextElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (disabled !== undefined) (el as unknown as UiCarouselNextElement).disabled = disabled;
        if (orientation !== undefined) (el as unknown as UiCarouselNextElement).orientation = orientation;
        }, [disabled, orientation]);

        return (
            <ui-carousel-next ref={elRef} {...htmlProps}>
                {children}
            </ui-carousel-next>
        );
    },
);

UiCarouselNext.displayName = 'UiCarouselNext';
