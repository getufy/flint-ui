// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/stepper/ui-stepper.ts

import React from 'react';
import type { UiStepContent as UiStepContentElement } from '../../../src/stepper/ui-stepper.js';
// Register the custom element
import '../../../src/stepper/ui-stepper.js';

export interface UiStepContentProps extends React.HTMLAttributes<HTMLElement> {
    open?: boolean;
}

export const UiStepContent = React.forwardRef<UiStepContentElement, UiStepContentProps>(
    function UiStepContent(
        { open, children, ...htmlProps }: UiStepContentProps,
        ref: React.ForwardedRef<UiStepContentElement>,
    ) {
        const innerRef = React.useRef<UiStepContentElement>(null);
        const elRef = (ref as React.RefObject<UiStepContentElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (open !== undefined) (el as unknown as UiStepContentElement).open = open;
        }, [open]);

        return (
            <ui-step-content ref={elRef} {...htmlProps}>
                {children}
            </ui-step-content>
        );
    },
);

UiStepContent.displayName = 'UiStepContent';
