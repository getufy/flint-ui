// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/item/ui-item.ts

import React from 'react';
import type { UiItem as UiItemElement } from '../../../src/item/ui-item.js';
// Register the custom element
import '../../../src/item/ui-item.js';

export interface UiItemProps extends React.HTMLAttributes<HTMLElement> {
    variant?: 'default' | 'outline' | 'muted';
    size?: 'default' | 'sm' | 'xs';
}

export const UiItem = React.forwardRef<UiItemElement, UiItemProps>(
    function UiItem(
        { variant, size, children, ...htmlProps }: UiItemProps,
        ref: React.ForwardedRef<UiItemElement>,
    ) {
        const innerRef = React.useRef<UiItemElement>(null);
        const elRef = (ref as React.RefObject<UiItemElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (variant !== undefined) (el as unknown as UiItemElement).variant = variant;
        if (size !== undefined) (el as unknown as UiItemElement).size = size;
        }, [variant, size]);

        return (
            <ui-item ref={elRef} {...htmlProps}>
                {children}
            </ui-item>
        );
    },
);

UiItem.displayName = 'UiItem';
