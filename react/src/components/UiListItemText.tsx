// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/list/ui-list.ts

import React from 'react';
import type { UiListItemText as UiListItemTextElement } from '../../../src/list/ui-list.js';
// Register the custom element
import '../../../src/list/ui-list.js';

export interface UiListItemTextProps extends React.HTMLAttributes<HTMLElement> {
    primary?: string;
    secondary?: string;
}

export const UiListItemText = React.forwardRef<UiListItemTextElement, UiListItemTextProps>(
    function UiListItemText(
        { primary, secondary, children, ...htmlProps }: UiListItemTextProps,
        ref: React.ForwardedRef<UiListItemTextElement>,
    ) {
        const innerRef = React.useRef<UiListItemTextElement>(null);
        const elRef = (ref as React.RefObject<UiListItemTextElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (primary !== undefined) (el as unknown as UiListItemTextElement).primary = primary;
        if (secondary !== undefined) (el as unknown as UiListItemTextElement).secondary = secondary;
        }, [primary, secondary]);

        return (
            <ui-list-item-text ref={elRef} {...htmlProps}>
                {children}
            </ui-list-item-text>
        );
    },
);

UiListItemText.displayName = 'UiListItemText';
