// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/menubar/ui-menubar.ts

import React from 'react';
import type { UiMenubarGroup as UiMenubarGroupElement } from '../../../src/menubar/ui-menubar.js';
// Register the custom element
import '../../../src/menubar/ui-menubar.js';

export interface UiMenubarGroupProps extends React.HTMLAttributes<HTMLElement> {
    heading?: string;
}

export const UiMenubarGroup = React.forwardRef<UiMenubarGroupElement, UiMenubarGroupProps>(
    function UiMenubarGroup(
        { heading, children, ...htmlProps }: UiMenubarGroupProps,
        ref: React.ForwardedRef<UiMenubarGroupElement>,
    ) {
        const innerRef = React.useRef<UiMenubarGroupElement>(null);
        const elRef = (ref as React.RefObject<UiMenubarGroupElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (heading !== undefined) (el as unknown as UiMenubarGroupElement).heading = heading;
        }, [heading]);

        return (
            <ui-menubar-group ref={elRef} {...htmlProps}>
                {children}
            </ui-menubar-group>
        );
    },
);

UiMenubarGroup.displayName = 'UiMenubarGroup';
