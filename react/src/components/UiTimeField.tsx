// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/time-picker/ui-time-picker.ts

import React from 'react';
import type { UiTimeField as UiTimeFieldElement } from '../../../src/time-picker/ui-time-picker.js';
import { UiTimeFieldEvents } from '../events/ui-time-field.js';
// Register the custom element
import '../../../src/time-picker/ui-time-picker.js';

export interface UiTimeFieldProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    label?: string;
    ampm?: boolean;
    seconds?: boolean;
    disabled?: boolean;
    readonly?: boolean;
    error?: boolean;
    helperText?: string;
    onClear?: (event: CustomEvent) => void;
    onChange?: (event: CustomEvent) => void;
}

export const UiTimeField = React.forwardRef<UiTimeFieldElement, UiTimeFieldProps>(
    function UiTimeField(
        { value, label, ampm, seconds, disabled, readonly, error, helperText, onClear, onChange, children, ...htmlProps }: UiTimeFieldProps,
        ref: React.ForwardedRef<UiTimeFieldElement>,
    ) {
        const innerRef = React.useRef<UiTimeFieldElement>(null);
        const elRef = (ref as React.RefObject<UiTimeFieldElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiTimeFieldElement).value = value;
        if (label !== undefined) (el as unknown as UiTimeFieldElement).label = label;
        if (ampm !== undefined) (el as unknown as UiTimeFieldElement).ampm = ampm;
        if (seconds !== undefined) (el as unknown as UiTimeFieldElement).seconds = seconds;
        if (disabled !== undefined) (el as unknown as UiTimeFieldElement).disabled = disabled;
        if (readonly !== undefined) (el as unknown as UiTimeFieldElement).readonly = readonly;
        if (error !== undefined) (el as unknown as UiTimeFieldElement).error = error;
        if (helperText !== undefined) (el as unknown as UiTimeFieldElement).helperText = helperText;
        }, [value, label, ampm, seconds, disabled, readonly, error, helperText]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onClear) return;
            const fn = onClear as EventListener;
            el.addEventListener(UiTimeFieldEvents.CLEAR, fn);
            return () => el.removeEventListener(UiTimeFieldEvents.CLEAR, fn);
        }, [onClear]);
    
        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiTimeFieldEvents.CHANGE, fn);
            return () => el.removeEventListener(UiTimeFieldEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-time-field ref={elRef} {...htmlProps}>
                {children}
            </ui-time-field>
        );
    },
);

UiTimeField.displayName = 'UiTimeField';
