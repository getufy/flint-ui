// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/kbd/ui-kbd.ts

import React from 'react';
import type { UiKbd as UiKbdElement } from '../../../src/kbd/ui-kbd.js';
// Register the custom element
import '../../../src/kbd/ui-kbd.js';

export interface UiKbdProps extends React.HTMLAttributes<HTMLElement> {
    size?: 'sm' | 'default' | 'lg';
    variant?: 'raised' | 'flat';
    label?: string;
}

export const UiKbd = React.forwardRef<UiKbdElement, UiKbdProps>(
    function UiKbd(
        { size, variant, label, children, ...htmlProps }: UiKbdProps,
        ref: React.ForwardedRef<UiKbdElement>,
    ) {
        const innerRef = React.useRef<UiKbdElement>(null);
        const elRef = (ref as React.RefObject<UiKbdElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (size !== undefined) (el as unknown as UiKbdElement).size = size;
        if (variant !== undefined) (el as unknown as UiKbdElement).variant = variant;
        if (label !== undefined) (el as unknown as UiKbdElement).label = label;
        }, [size, variant, label]);

        return (
            <ui-kbd ref={elRef} {...htmlProps}>
                {children}
            </ui-kbd>
        );
    },
);

UiKbd.displayName = 'UiKbd';
