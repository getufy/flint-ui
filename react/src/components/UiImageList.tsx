// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/image-list/ui-image-list.ts

import React from 'react';
import type { UiImageList as UiImageListElement } from '../../../src/image-list/ui-image-list.js';
// Register the custom element
import '../../../src/image-list/ui-image-list.js';

export interface UiImageListProps extends React.HTMLAttributes<HTMLElement> {
    variant?: ImageListVariant;
    cols?: number;
    gap?: number;
    rowHeight?: number;
    autoRows?: boolean;
}

export const UiImageList = React.forwardRef<UiImageListElement, UiImageListProps>(
    function UiImageList(
        { variant, cols, gap, rowHeight, autoRows, children, ...htmlProps }: UiImageListProps,
        ref: React.ForwardedRef<UiImageListElement>,
    ) {
        const innerRef = React.useRef<UiImageListElement>(null);
        const elRef = (ref as React.RefObject<UiImageListElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (variant !== undefined) (el as unknown as UiImageListElement).variant = variant;
        if (cols !== undefined) (el as unknown as UiImageListElement).cols = cols;
        if (gap !== undefined) (el as unknown as UiImageListElement).gap = gap;
        if (rowHeight !== undefined) (el as unknown as UiImageListElement).rowHeight = rowHeight;
        if (autoRows !== undefined) (el as unknown as UiImageListElement).autoRows = autoRows;
        }, [variant, cols, gap, rowHeight, autoRows]);

        return (
            <ui-image-list ref={elRef} {...htmlProps}>
                {children}
            </ui-image-list>
        );
    },
);

UiImageList.displayName = 'UiImageList';
