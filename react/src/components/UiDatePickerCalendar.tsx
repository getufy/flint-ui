// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/date-picker/ui-date-picker.ts

import React from 'react';
import type { UiDatePickerCalendar as UiDatePickerCalendarElement } from '../../../src/date-picker/ui-date-picker.js';
import { UiDatePickerCalendarEvents } from '../events/ui-date-picker-calendar.js';
// Register the custom element
import '../../../src/date-picker/ui-date-picker.js';

export interface UiDatePickerCalendarProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    min?: string;
    max?: string;
    disabled?: boolean;
    onDateSelect?: (event: CustomEvent) => void;
}

export const UiDatePickerCalendar = React.forwardRef<UiDatePickerCalendarElement, UiDatePickerCalendarProps>(
    function UiDatePickerCalendar(
        { value, min, max, disabled, onDateSelect, children, ...htmlProps }: UiDatePickerCalendarProps,
        ref: React.ForwardedRef<UiDatePickerCalendarElement>,
    ) {
        const innerRef = React.useRef<UiDatePickerCalendarElement>(null);
        const elRef = (ref as React.RefObject<UiDatePickerCalendarElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiDatePickerCalendarElement).value = value;
        if (min !== undefined) (el as unknown as UiDatePickerCalendarElement).min = min;
        if (max !== undefined) (el as unknown as UiDatePickerCalendarElement).max = max;
        if (disabled !== undefined) (el as unknown as UiDatePickerCalendarElement).disabled = disabled;
        }, [value, min, max, disabled]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onDateSelect) return;
            const fn = onDateSelect as EventListener;
            el.addEventListener(UiDatePickerCalendarEvents.DATE_SELECT, fn);
            return () => el.removeEventListener(UiDatePickerCalendarEvents.DATE_SELECT, fn);
        }, [onDateSelect]);
    
        return (
            <ui-date-picker-calendar ref={elRef} {...htmlProps}>
                {children}
            </ui-date-picker-calendar>
        );
    },
);

UiDatePickerCalendar.displayName = 'UiDatePickerCalendar';
