// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/checkbox/ui-checkbox.ts

import React from 'react';
import type { UiCheckbox as UiCheckboxElement } from '../../../src/checkbox/ui-checkbox.js';
import { UiCheckboxEvents } from '../events/ui-checkbox.js';
// Register the custom element
import '../../../src/checkbox/ui-checkbox.js';

export interface UiCheckboxProps extends React.HTMLAttributes<HTMLElement> {
    checked?: boolean;
    indeterminate?: boolean;
    disabled?: boolean;
    required?: boolean;
    label?: string;
    name?: string;
    value?: string;
    onChange?: (event: CustomEvent) => void;
}

export const UiCheckbox = React.forwardRef<UiCheckboxElement, UiCheckboxProps>(
    function UiCheckbox(
        { checked, indeterminate, disabled, required, label, name, value, onChange, children, ...htmlProps }: UiCheckboxProps,
        ref: React.ForwardedRef<UiCheckboxElement>,
    ) {
        const innerRef = React.useRef<UiCheckboxElement>(null);
        const elRef = (ref as React.RefObject<UiCheckboxElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (checked !== undefined) (el as unknown as UiCheckboxElement).checked = checked;
        if (indeterminate !== undefined) (el as unknown as UiCheckboxElement).indeterminate = indeterminate;
        if (disabled !== undefined) (el as unknown as UiCheckboxElement).disabled = disabled;
        if (required !== undefined) (el as unknown as UiCheckboxElement).required = required;
        if (label !== undefined) (el as unknown as UiCheckboxElement).label = label;
        if (name !== undefined) (el as unknown as UiCheckboxElement).name = name;
        if (value !== undefined) (el as unknown as UiCheckboxElement).value = value;
        }, [checked, indeterminate, disabled, required, label, name, value]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiCheckboxEvents.CHANGE, fn);
            return () => el.removeEventListener(UiCheckboxEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-checkbox ref={elRef} {...htmlProps}>
                {children}
            </ui-checkbox>
        );
    },
);

UiCheckbox.displayName = 'UiCheckbox';
