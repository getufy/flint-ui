// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/select/ui-select.ts

import React from 'react';
import type { UiSelect as UiSelectElement } from '../../../src/select/ui-select.js';
import { UiSelectEvents } from '../events/ui-select.js';
// Register the custom element
import '../../../src/select/ui-select.js';

export interface UiSelectProps extends React.HTMLAttributes<HTMLElement> {
    label?: string;
    options?: SelectOption[];
    value?: string[];
    multiple?: boolean;
    placeholder?: string;
    disabled?: boolean;
    readonly?: boolean;
    required?: boolean;
    error?: boolean;
    errorMessage?: string;
    name?: string;
    size?: SelectSize;
    defaultValue?: string;
    onChange?: (event: CustomEvent) => void;
}

export const UiSelect = React.forwardRef<UiSelectElement, UiSelectProps>(
    function UiSelect(
        { label, options, value, multiple, placeholder, disabled, readonly, required, error, errorMessage, name, size, defaultValue, onChange, children, ...htmlProps }: UiSelectProps,
        ref: React.ForwardedRef<UiSelectElement>,
    ) {
        const innerRef = React.useRef<UiSelectElement>(null);
        const elRef = (ref as React.RefObject<UiSelectElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (label !== undefined) (el as unknown as UiSelectElement).label = label;
        if (options !== undefined) (el as unknown as UiSelectElement).options = options;
        if (value !== undefined) (el as unknown as UiSelectElement).value = value;
        if (multiple !== undefined) (el as unknown as UiSelectElement).multiple = multiple;
        if (placeholder !== undefined) (el as unknown as UiSelectElement).placeholder = placeholder;
        if (disabled !== undefined) (el as unknown as UiSelectElement).disabled = disabled;
        if (readonly !== undefined) (el as unknown as UiSelectElement).readonly = readonly;
        if (required !== undefined) (el as unknown as UiSelectElement).required = required;
        if (error !== undefined) (el as unknown as UiSelectElement).error = error;
        if (errorMessage !== undefined) (el as unknown as UiSelectElement).errorMessage = errorMessage;
        if (name !== undefined) (el as unknown as UiSelectElement).name = name;
        if (size !== undefined) (el as unknown as UiSelectElement).size = size;
        if (defaultValue !== undefined) (el as unknown as UiSelectElement).defaultValue = defaultValue;
        }, [label, options, value, multiple, placeholder, disabled, readonly, required, error, errorMessage, name, size, defaultValue]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiSelectEvents.CHANGE, fn);
            return () => el.removeEventListener(UiSelectEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-select ref={elRef} {...htmlProps}>
                {children}
            </ui-select>
        );
    },
);

UiSelect.displayName = 'UiSelect';
