// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/date-range-picker/ui-date-range-calendar.ts

import React from 'react';
import type { UiDateRangeCalendar as UiDateRangeCalendarElement } from '../../../src/date-range-picker/ui-date-range-calendar.js';
import { UiDateRangeCalendarEvents } from '../events/ui-date-range-calendar.js';
// Register the custom element
import '../../../src/date-range-picker/ui-date-range-calendar.js';

export interface UiDateRangeCalendarProps extends React.HTMLAttributes<HTMLElement> {
    value?: DateRange;
    min?: string;
    max?: string;
    disabled?: boolean;
    onRangeSelect?: (event: CustomEvent) => void;
}

export const UiDateRangeCalendar = React.forwardRef<UiDateRangeCalendarElement, UiDateRangeCalendarProps>(
    function UiDateRangeCalendar(
        { value, min, max, disabled, onRangeSelect, children, ...htmlProps }: UiDateRangeCalendarProps,
        ref: React.ForwardedRef<UiDateRangeCalendarElement>,
    ) {
        const innerRef = React.useRef<UiDateRangeCalendarElement>(null);
        const elRef = (ref as React.RefObject<UiDateRangeCalendarElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiDateRangeCalendarElement).value = value;
        if (min !== undefined) (el as unknown as UiDateRangeCalendarElement).min = min;
        if (max !== undefined) (el as unknown as UiDateRangeCalendarElement).max = max;
        if (disabled !== undefined) (el as unknown as UiDateRangeCalendarElement).disabled = disabled;
        }, [value, min, max, disabled]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onRangeSelect) return;
            const fn = onRangeSelect as EventListener;
            el.addEventListener(UiDateRangeCalendarEvents.RANGE_SELECT, fn);
            return () => el.removeEventListener(UiDateRangeCalendarEvents.RANGE_SELECT, fn);
        }, [onRangeSelect]);
    
        return (
            <ui-date-range-calendar ref={elRef} {...htmlProps}>
                {children}
            </ui-date-range-calendar>
        );
    },
);

UiDateRangeCalendar.displayName = 'UiDateRangeCalendar';
