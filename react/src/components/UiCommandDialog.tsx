// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/command/ui-command.ts

import React from 'react';
import type { UiCommandDialog as UiCommandDialogElement } from '../../../src/command/ui-command.js';
import { UiCommandDialogEvents } from '../events/ui-command-dialog.js';
// Register the custom element
import '../../../src/command/ui-command.js';

export interface UiCommandDialogProps extends React.HTMLAttributes<HTMLElement> {
    open?: boolean;
    onUiCommandDialogClose?: (event: CustomEvent) => void;
}

export const UiCommandDialog = React.forwardRef<UiCommandDialogElement, UiCommandDialogProps>(
    function UiCommandDialog(
        { open, onUiCommandDialogClose, children, ...htmlProps }: UiCommandDialogProps,
        ref: React.ForwardedRef<UiCommandDialogElement>,
    ) {
        const innerRef = React.useRef<UiCommandDialogElement>(null);
        const elRef = (ref as React.RefObject<UiCommandDialogElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (open !== undefined) (el as unknown as UiCommandDialogElement).open = open;
        }, [open]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiCommandDialogClose) return;
            const fn = onUiCommandDialogClose as EventListener;
            el.addEventListener(UiCommandDialogEvents.CLOSE, fn);
            return () => el.removeEventListener(UiCommandDialogEvents.CLOSE, fn);
        }, [onUiCommandDialogClose]);
    
        return (
            <ui-command-dialog ref={elRef} {...htmlProps}>
                {children}
            </ui-command-dialog>
        );
    },
);

UiCommandDialog.displayName = 'UiCommandDialog';
