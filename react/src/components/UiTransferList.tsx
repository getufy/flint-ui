// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/transfer-list/ui-transfer-list.ts

import React from 'react';
import type { UiTransferList as UiTransferListElement } from '../../../src/transfer-list/ui-transfer-list.js';
import { UiTransferListEvents } from '../events/ui-transfer-list.js';
// Register the custom element
import '../../../src/transfer-list/ui-transfer-list.js';

export interface UiTransferListProps extends React.HTMLAttributes<HTMLElement> {
    options?: TransferOption[];
    value?: string[];
    leftTitle?: string;
    rightTitle?: string;
    onChange?: (event: CustomEvent) => void;
}

export const UiTransferList = React.forwardRef<UiTransferListElement, UiTransferListProps>(
    function UiTransferList(
        { options, value, leftTitle, rightTitle, onChange, children, ...htmlProps }: UiTransferListProps,
        ref: React.ForwardedRef<UiTransferListElement>,
    ) {
        const innerRef = React.useRef<UiTransferListElement>(null);
        const elRef = (ref as React.RefObject<UiTransferListElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (options !== undefined) (el as unknown as UiTransferListElement).options = options;
        if (value !== undefined) (el as unknown as UiTransferListElement).value = value;
        if (leftTitle !== undefined) (el as unknown as UiTransferListElement).leftTitle = leftTitle;
        if (rightTitle !== undefined) (el as unknown as UiTransferListElement).rightTitle = rightTitle;
        }, [options, value, leftTitle, rightTitle]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiTransferListEvents.CHANGE, fn);
            return () => el.removeEventListener(UiTransferListEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-transfer-list ref={elRef} {...htmlProps}>
                {children}
            </ui-transfer-list>
        );
    },
);

UiTransferList.displayName = 'UiTransferList';
