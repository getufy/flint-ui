// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/stepper/ui-stepper.ts

import React from 'react';
import type { UiStep as UiStepElement } from '../../../src/stepper/ui-stepper.js';
import { UiStepEvents } from '../events/ui-step.js';
// Register the custom element
import '../../../src/stepper/ui-stepper.js';

export interface UiStepProps extends React.HTMLAttributes<HTMLElement> {
    active?: boolean;
    completed?: boolean;
    disabled?: boolean;
    optional?: boolean;
    error?: boolean;
    last?: boolean;
    clickable?: boolean;
    orientation?: 'horizontal' | 'vertical';
    alternativeLabel?: boolean;
    stepIndex?: number;
    optionalLabel?: string;
    prevCompleted?: boolean;
    onUiStepClick?: (event: CustomEvent) => void;
}

export const UiStep = React.forwardRef<UiStepElement, UiStepProps>(
    function UiStep(
        { active, completed, disabled, optional, error, last, clickable, orientation, alternativeLabel, stepIndex, optionalLabel, prevCompleted, onUiStepClick, children, ...htmlProps }: UiStepProps,
        ref: React.ForwardedRef<UiStepElement>,
    ) {
        const innerRef = React.useRef<UiStepElement>(null);
        const elRef = (ref as React.RefObject<UiStepElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (active !== undefined) (el as unknown as UiStepElement).active = active;
        if (completed !== undefined) (el as unknown as UiStepElement).completed = completed;
        if (disabled !== undefined) (el as unknown as UiStepElement).disabled = disabled;
        if (optional !== undefined) (el as unknown as UiStepElement).optional = optional;
        if (error !== undefined) (el as unknown as UiStepElement).error = error;
        if (last !== undefined) (el as unknown as UiStepElement).last = last;
        if (clickable !== undefined) (el as unknown as UiStepElement).clickable = clickable;
        if (orientation !== undefined) (el as unknown as UiStepElement).orientation = orientation;
        if (alternativeLabel !== undefined) (el as unknown as UiStepElement).alternativeLabel = alternativeLabel;
        if (stepIndex !== undefined) (el as unknown as UiStepElement).stepIndex = stepIndex;
        if (optionalLabel !== undefined) (el as unknown as UiStepElement).optionalLabel = optionalLabel;
        if (prevCompleted !== undefined) (el as unknown as UiStepElement).prevCompleted = prevCompleted;
        }, [active, completed, disabled, optional, error, last, clickable, orientation, alternativeLabel, stepIndex, optionalLabel, prevCompleted]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiStepClick) return;
            const fn = onUiStepClick as EventListener;
            el.addEventListener(UiStepEvents.CLICK, fn);
            return () => el.removeEventListener(UiStepEvents.CLICK, fn);
        }, [onUiStepClick]);
    
        return (
            <ui-step ref={elRef} {...htmlProps}>
                {children}
            </ui-step>
        );
    },
);

UiStep.displayName = 'UiStep';
