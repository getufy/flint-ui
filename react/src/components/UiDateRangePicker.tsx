// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/date-range-picker/ui-date-range-picker.ts

import React from 'react';
import type { UiDateRangePicker as UiDateRangePickerElement } from '../../../src/date-range-picker/ui-date-range-picker.js';
import { UiDateRangePickerEvents } from '../events/ui-date-range-picker.js';
// Register the custom element
import '../../../src/date-range-picker/ui-date-range-picker.js';

export interface UiDateRangePickerProps extends React.HTMLAttributes<HTMLElement> {
    value?: DateRange;
    label?: string;
    variant?: 'desktop' | 'mobile' | 'static' | 'auto';
    multiInput?: boolean;
    shortcuts?: boolean;
    shortcutItems?: Shortcut[];
    min?: string;
    max?: string;
    name?: string;
    disabled?: boolean;
    readonly?: boolean;
    error?: boolean;
    helperText?: string;
    onRangeChange?: (event: CustomEvent) => void;
}

export const UiDateRangePicker = React.forwardRef<UiDateRangePickerElement, UiDateRangePickerProps>(
    function UiDateRangePicker(
        { value, label, variant, multiInput, shortcuts, shortcutItems, min, max, name, disabled, readonly, error, helperText, onRangeChange, children, ...htmlProps }: UiDateRangePickerProps,
        ref: React.ForwardedRef<UiDateRangePickerElement>,
    ) {
        const innerRef = React.useRef<UiDateRangePickerElement>(null);
        const elRef = (ref as React.RefObject<UiDateRangePickerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiDateRangePickerElement).value = value;
        if (label !== undefined) (el as unknown as UiDateRangePickerElement).label = label;
        if (variant !== undefined) (el as unknown as UiDateRangePickerElement).variant = variant;
        if (multiInput !== undefined) (el as unknown as UiDateRangePickerElement).multiInput = multiInput;
        if (shortcuts !== undefined) (el as unknown as UiDateRangePickerElement).shortcuts = shortcuts;
        if (shortcutItems !== undefined) (el as unknown as UiDateRangePickerElement).shortcutItems = shortcutItems;
        if (min !== undefined) (el as unknown as UiDateRangePickerElement).min = min;
        if (max !== undefined) (el as unknown as UiDateRangePickerElement).max = max;
        if (name !== undefined) (el as unknown as UiDateRangePickerElement).name = name;
        if (disabled !== undefined) (el as unknown as UiDateRangePickerElement).disabled = disabled;
        if (readonly !== undefined) (el as unknown as UiDateRangePickerElement).readonly = readonly;
        if (error !== undefined) (el as unknown as UiDateRangePickerElement).error = error;
        if (helperText !== undefined) (el as unknown as UiDateRangePickerElement).helperText = helperText;
        }, [value, label, variant, multiInput, shortcuts, shortcutItems, min, max, name, disabled, readonly, error, helperText]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onRangeChange) return;
            const fn = onRangeChange as EventListener;
            el.addEventListener(UiDateRangePickerEvents.RANGE_CHANGE, fn);
            return () => el.removeEventListener(UiDateRangePickerEvents.RANGE_CHANGE, fn);
        }, [onRangeChange]);
    
        return (
            <ui-date-range-picker ref={elRef} {...htmlProps}>
                {children}
            </ui-date-range-picker>
        );
    },
);

UiDateRangePicker.displayName = 'UiDateRangePicker';
