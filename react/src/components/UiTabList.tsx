// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/tabs/ui-tabs.ts

import React from 'react';
import type { UiTabList as UiTabListElement } from '../../../src/tabs/ui-tabs.js';
import { UiTabListEvents } from '../events/ui-tab-list.js';
// Register the custom element
import '../../../src/tabs/ui-tabs.js';

export interface UiTabListProps extends React.HTMLAttributes<HTMLElement> {
    orientation?: 'horizontal' | 'vertical';
    variant?: 'standard' | 'fullWidth' | 'scrollable';
    centered?: boolean;
    scrollButtons?: 'auto' | 'false';
    onUiTabClick?: (event: CustomEvent) => void;
}

export const UiTabList = React.forwardRef<UiTabListElement, UiTabListProps>(
    function UiTabList(
        { orientation, variant, centered, scrollButtons, onUiTabClick, children, ...htmlProps }: UiTabListProps,
        ref: React.ForwardedRef<UiTabListElement>,
    ) {
        const innerRef = React.useRef<UiTabListElement>(null);
        const elRef = (ref as React.RefObject<UiTabListElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (orientation !== undefined) (el as unknown as UiTabListElement).orientation = orientation;
        if (variant !== undefined) (el as unknown as UiTabListElement).variant = variant;
        if (centered !== undefined) (el as unknown as UiTabListElement).centered = centered;
        if (scrollButtons !== undefined) (el as unknown as UiTabListElement).scrollButtons = scrollButtons;
        }, [orientation, variant, centered, scrollButtons]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiTabClick) return;
            const fn = onUiTabClick as EventListener;
            el.addEventListener(UiTabListEvents.TAB_CLICK, fn);
            return () => el.removeEventListener(UiTabListEvents.TAB_CLICK, fn);
        }, [onUiTabClick]);
    
        return (
            <ui-tab-list ref={elRef} {...htmlProps}>
                {children}
            </ui-tab-list>
        );
    },
);

UiTabList.displayName = 'UiTabList';
