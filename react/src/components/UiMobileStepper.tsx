// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/stepper/ui-stepper.ts

import React from 'react';
import type { UiMobileStepper as UiMobileStepperElement } from '../../../src/stepper/ui-stepper.js';
// Register the custom element
import '../../../src/stepper/ui-stepper.js';

export interface UiMobileStepperProps extends React.HTMLAttributes<HTMLElement> {
    steps?: number;
    activeStep?: number;
    variant?: 'text' | 'dots' | 'progress';
    position?: 'top' | 'bottom' | 'static';
    backLabel?: string;
    nextLabel?: string;
}

export const UiMobileStepper = React.forwardRef<UiMobileStepperElement, UiMobileStepperProps>(
    function UiMobileStepper(
        { steps, activeStep, variant, position, backLabel, nextLabel, children, ...htmlProps }: UiMobileStepperProps,
        ref: React.ForwardedRef<UiMobileStepperElement>,
    ) {
        const innerRef = React.useRef<UiMobileStepperElement>(null);
        const elRef = (ref as React.RefObject<UiMobileStepperElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (steps !== undefined) (el as unknown as UiMobileStepperElement).steps = steps;
        if (activeStep !== undefined) (el as unknown as UiMobileStepperElement).activeStep = activeStep;
        if (variant !== undefined) (el as unknown as UiMobileStepperElement).variant = variant;
        if (position !== undefined) (el as unknown as UiMobileStepperElement).position = position;
        if (backLabel !== undefined) (el as unknown as UiMobileStepperElement).backLabel = backLabel;
        if (nextLabel !== undefined) (el as unknown as UiMobileStepperElement).nextLabel = nextLabel;
        }, [steps, activeStep, variant, position, backLabel, nextLabel]);

        return (
            <ui-mobile-stepper ref={elRef} {...htmlProps}>
                {children}
            </ui-mobile-stepper>
        );
    },
);

UiMobileStepper.displayName = 'UiMobileStepper';
