// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/speed-dial/ui-speed-dial.ts

import React from 'react';
import type { UiSpeedDialAction as UiSpeedDialActionElement } from '../../../src/speed-dial/ui-speed-dial.js';
import { UiSpeedDialActionEvents } from '../events/ui-speed-dial-action.js';
// Register the custom element
import '../../../src/speed-dial/ui-speed-dial.js';

export interface UiSpeedDialActionProps extends React.HTMLAttributes<HTMLElement> {
    name?: string;
    tooltipTitle?: string;
    tooltipOpen?: boolean;
    tooltipPlacement?: 'left' | 'right' | 'top' | 'bottom';
    disabled?: boolean;
    onUiSpeedDialActionClick?: (event: CustomEvent) => void;
}

export const UiSpeedDialAction = React.forwardRef<UiSpeedDialActionElement, UiSpeedDialActionProps>(
    function UiSpeedDialAction(
        { name, tooltipTitle, tooltipOpen, tooltipPlacement, disabled, onUiSpeedDialActionClick, children, ...htmlProps }: UiSpeedDialActionProps,
        ref: React.ForwardedRef<UiSpeedDialActionElement>,
    ) {
        const innerRef = React.useRef<UiSpeedDialActionElement>(null);
        const elRef = (ref as React.RefObject<UiSpeedDialActionElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (name !== undefined) (el as unknown as UiSpeedDialActionElement).name = name;
        if (tooltipTitle !== undefined) (el as unknown as UiSpeedDialActionElement).tooltipTitle = tooltipTitle;
        if (tooltipOpen !== undefined) (el as unknown as UiSpeedDialActionElement).tooltipOpen = tooltipOpen;
        if (tooltipPlacement !== undefined) (el as unknown as UiSpeedDialActionElement).tooltipPlacement = tooltipPlacement;
        if (disabled !== undefined) (el as unknown as UiSpeedDialActionElement).disabled = disabled;
        }, [name, tooltipTitle, tooltipOpen, tooltipPlacement, disabled]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiSpeedDialActionClick) return;
            const fn = onUiSpeedDialActionClick as EventListener;
            el.addEventListener(UiSpeedDialActionEvents.CLICK, fn);
            return () => el.removeEventListener(UiSpeedDialActionEvents.CLICK, fn);
        }, [onUiSpeedDialActionClick]);
    
        return (
            <ui-speed-dial-action ref={elRef} {...htmlProps}>
                {children}
            </ui-speed-dial-action>
        );
    },
);

UiSpeedDialAction.displayName = 'UiSpeedDialAction';
