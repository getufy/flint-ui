// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/table/ui-table.ts

import React from 'react';
import type { UiTableRow as UiTableRowElement } from '../../../src/table/ui-table.js';
// Register the custom element
import '../../../src/table/ui-table.js';

export interface UiTableRowProps extends React.HTMLAttributes<HTMLElement> {
    selected?: boolean;
}

export const UiTableRow = React.forwardRef<UiTableRowElement, UiTableRowProps>(
    function UiTableRow(
        { selected, children, ...htmlProps }: UiTableRowProps,
        ref: React.ForwardedRef<UiTableRowElement>,
    ) {
        const innerRef = React.useRef<UiTableRowElement>(null);
        const elRef = (ref as React.RefObject<UiTableRowElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (selected !== undefined) (el as unknown as UiTableRowElement).selected = selected;
        }, [selected]);

        return (
            <ui-table-row ref={elRef} {...htmlProps}>
                {children}
            </ui-table-row>
        );
    },
);

UiTableRow.displayName = 'UiTableRow';
