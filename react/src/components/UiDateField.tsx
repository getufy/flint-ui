// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/date-field/ui-date-field.ts

import React from 'react';
import type { UiDateField as UiDateFieldElement } from '../../../src/date-field/ui-date-field.js';
import { UiDateFieldEvents } from '../events/ui-date-field.js';
// Register the custom element
import '../../../src/date-field/ui-date-field.js';

export interface UiDateFieldProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    label?: string;
    name?: string;
    min?: string;
    max?: string;
    disabled?: boolean;
    readonly?: boolean;
    error?: boolean;
    helperText?: string;
    onClear?: (event: CustomEvent) => void;
    onChange?: (event: CustomEvent) => void;
}

export const UiDateField = React.forwardRef<UiDateFieldElement, UiDateFieldProps>(
    function UiDateField(
        { value, label, name, min, max, disabled, readonly, error, helperText, onClear, onChange, children, ...htmlProps }: UiDateFieldProps,
        ref: React.ForwardedRef<UiDateFieldElement>,
    ) {
        const innerRef = React.useRef<UiDateFieldElement>(null);
        const elRef = (ref as React.RefObject<UiDateFieldElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiDateFieldElement).value = value;
        if (label !== undefined) (el as unknown as UiDateFieldElement).label = label;
        if (name !== undefined) (el as unknown as UiDateFieldElement).name = name;
        if (min !== undefined) (el as unknown as UiDateFieldElement).min = min;
        if (max !== undefined) (el as unknown as UiDateFieldElement).max = max;
        if (disabled !== undefined) (el as unknown as UiDateFieldElement).disabled = disabled;
        if (readonly !== undefined) (el as unknown as UiDateFieldElement).readonly = readonly;
        if (error !== undefined) (el as unknown as UiDateFieldElement).error = error;
        if (helperText !== undefined) (el as unknown as UiDateFieldElement).helperText = helperText;
        }, [value, label, name, min, max, disabled, readonly, error, helperText]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onClear) return;
            const fn = onClear as EventListener;
            el.addEventListener(UiDateFieldEvents.CLEAR, fn);
            return () => el.removeEventListener(UiDateFieldEvents.CLEAR, fn);
        }, [onClear]);
    
        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiDateFieldEvents.CHANGE, fn);
            return () => el.removeEventListener(UiDateFieldEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-date-field ref={elRef} {...htmlProps}>
                {children}
            </ui-date-field>
        );
    },
);

UiDateField.displayName = 'UiDateField';
