// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/image-list/ui-image-list-item-bar.ts

import React from 'react';
import type { UiImageListItemBar as UiImageListItemBarElement } from '../../../src/image-list/ui-image-list-item-bar.js';
// Register the custom element
import '../../../src/image-list/ui-image-list-item-bar.js';

export interface UiImageListItemBarProps extends React.HTMLAttributes<HTMLElement> {
    position?: 'bottom' | 'top' | 'below';
}

export const UiImageListItemBar = React.forwardRef<UiImageListItemBarElement, UiImageListItemBarProps>(
    function UiImageListItemBar(
        { position, children, ...htmlProps }: UiImageListItemBarProps,
        ref: React.ForwardedRef<UiImageListItemBarElement>,
    ) {
        const innerRef = React.useRef<UiImageListItemBarElement>(null);
        const elRef = (ref as React.RefObject<UiImageListItemBarElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (position !== undefined) (el as unknown as UiImageListItemBarElement).position = position;
        }, [position]);

        return (
            <ui-image-list-item-bar ref={elRef} {...htmlProps}>
                {children}
            </ui-image-list-item-bar>
        );
    },
);

UiImageListItemBar.displayName = 'UiImageListItemBar';
