// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/card/ui-card.ts

import React from 'react';
import type { UiCard as UiCardElement } from '../../../src/card/ui-card.js';
// Register the custom element
import '../../../src/card/ui-card.js';

export interface UiCardProps extends React.HTMLAttributes<HTMLElement> {
    variant?: 'elevated' | 'outlined' | 'flat';
    interactive?: boolean;
}

export const UiCard = React.forwardRef<UiCardElement, UiCardProps>(
    function UiCard(
        { variant, interactive, children, ...htmlProps }: UiCardProps,
        ref: React.ForwardedRef<UiCardElement>,
    ) {
        const innerRef = React.useRef<UiCardElement>(null);
        const elRef = (ref as React.RefObject<UiCardElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (variant !== undefined) (el as unknown as UiCardElement).variant = variant;
        if (interactive !== undefined) (el as unknown as UiCardElement).interactive = interactive;
        }, [variant, interactive]);

        return (
            <ui-card ref={elRef} {...htmlProps}>
                {children}
            </ui-card>
        );
    },
);

UiCard.displayName = 'UiCard';
