// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/switch/ui-switch.ts

import React from 'react';
import type { UiSwitch as UiSwitchElement } from '../../../src/switch/ui-switch.js';
import { UiSwitchEvents } from '../events/ui-switch.js';
// Register the custom element
import '../../../src/switch/ui-switch.js';

export interface UiSwitchProps extends React.HTMLAttributes<HTMLElement> {
    checked?: boolean;
    disabled?: boolean;
    required?: boolean;
    size?: 'sm' | 'md' | 'lg';
    label?: string;
    name?: string;
    value?: string;
    defaultChecked?: boolean;
    ariaLabel?: string | null;
    onUiSwitchChange?: (event: CustomEvent) => void;
}

export const UiSwitch = React.forwardRef<UiSwitchElement, UiSwitchProps>(
    function UiSwitch(
        { checked, disabled, required, size, label, name, value, defaultChecked, ariaLabel, onUiSwitchChange, children, ...htmlProps }: UiSwitchProps,
        ref: React.ForwardedRef<UiSwitchElement>,
    ) {
        const innerRef = React.useRef<UiSwitchElement>(null);
        const elRef = (ref as React.RefObject<UiSwitchElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (checked !== undefined) (el as unknown as UiSwitchElement).checked = checked;
        if (disabled !== undefined) (el as unknown as UiSwitchElement).disabled = disabled;
        if (required !== undefined) (el as unknown as UiSwitchElement).required = required;
        if (size !== undefined) (el as unknown as UiSwitchElement).size = size;
        if (label !== undefined) (el as unknown as UiSwitchElement).label = label;
        if (name !== undefined) (el as unknown as UiSwitchElement).name = name;
        if (value !== undefined) (el as unknown as UiSwitchElement).value = value;
        if (defaultChecked !== undefined) (el as unknown as UiSwitchElement).defaultChecked = defaultChecked;
        if (ariaLabel !== undefined) (el as unknown as UiSwitchElement).ariaLabel = ariaLabel;
        }, [checked, disabled, required, size, label, name, value, defaultChecked, ariaLabel]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiSwitchChange) return;
            const fn = onUiSwitchChange as EventListener;
            el.addEventListener(UiSwitchEvents.CHANGE, fn);
            return () => el.removeEventListener(UiSwitchEvents.CHANGE, fn);
        }, [onUiSwitchChange]);
    
        return (
            <ui-switch ref={elRef} {...htmlProps}>
                {children}
            </ui-switch>
        );
    },
);

UiSwitch.displayName = 'UiSwitch';
