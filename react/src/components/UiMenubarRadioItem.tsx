// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/menubar/ui-menubar.ts

import React from 'react';
import type { UiMenubarRadioItem as UiMenubarRadioItemElement } from '../../../src/menubar/ui-menubar.js';
// Register the custom element
import '../../../src/menubar/ui-menubar.js';

export interface UiMenubarRadioItemProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    checked?: boolean;
    disabled?: boolean;
    highlighted?: boolean;
}

export const UiMenubarRadioItem = React.forwardRef<UiMenubarRadioItemElement, UiMenubarRadioItemProps>(
    function UiMenubarRadioItem(
        { value, checked, disabled, highlighted, children, ...htmlProps }: UiMenubarRadioItemProps,
        ref: React.ForwardedRef<UiMenubarRadioItemElement>,
    ) {
        const innerRef = React.useRef<UiMenubarRadioItemElement>(null);
        const elRef = (ref as React.RefObject<UiMenubarRadioItemElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiMenubarRadioItemElement).value = value;
        if (checked !== undefined) (el as unknown as UiMenubarRadioItemElement).checked = checked;
        if (disabled !== undefined) (el as unknown as UiMenubarRadioItemElement).disabled = disabled;
        if (highlighted !== undefined) (el as unknown as UiMenubarRadioItemElement).highlighted = highlighted;
        }, [value, checked, disabled, highlighted]);

        return (
            <ui-menubar-radio-item ref={elRef} {...htmlProps}>
                {children}
            </ui-menubar-radio-item>
        );
    },
);

UiMenubarRadioItem.displayName = 'UiMenubarRadioItem';
