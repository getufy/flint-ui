// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/date-picker/ui-date-picker.ts

import React from 'react';
import type { UiDatePicker as UiDatePickerElement } from '../../../src/date-picker/ui-date-picker.js';
import { UiDatePickerEvents } from '../events/ui-date-picker.js';
// Register the custom element
import '../../../src/date-picker/ui-date-picker.js';

export interface UiDatePickerProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    label?: string;
    placeholder?: string;
    name?: string;
    variant?: 'desktop' | 'mobile' | 'static' | 'auto';
    min?: string;
    max?: string;
    disabled?: boolean;
    readonly?: boolean;
    error?: boolean;
    helperText?: string;
    onChange?: (event: CustomEvent) => void;
}

export const UiDatePicker = React.forwardRef<UiDatePickerElement, UiDatePickerProps>(
    function UiDatePicker(
        { value, label, placeholder, name, variant, min, max, disabled, readonly, error, helperText, onChange, children, ...htmlProps }: UiDatePickerProps,
        ref: React.ForwardedRef<UiDatePickerElement>,
    ) {
        const innerRef = React.useRef<UiDatePickerElement>(null);
        const elRef = (ref as React.RefObject<UiDatePickerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiDatePickerElement).value = value;
        if (label !== undefined) (el as unknown as UiDatePickerElement).label = label;
        if (placeholder !== undefined) (el as unknown as UiDatePickerElement).placeholder = placeholder;
        if (name !== undefined) (el as unknown as UiDatePickerElement).name = name;
        if (variant !== undefined) (el as unknown as UiDatePickerElement).variant = variant;
        if (min !== undefined) (el as unknown as UiDatePickerElement).min = min;
        if (max !== undefined) (el as unknown as UiDatePickerElement).max = max;
        if (disabled !== undefined) (el as unknown as UiDatePickerElement).disabled = disabled;
        if (readonly !== undefined) (el as unknown as UiDatePickerElement).readonly = readonly;
        if (error !== undefined) (el as unknown as UiDatePickerElement).error = error;
        if (helperText !== undefined) (el as unknown as UiDatePickerElement).helperText = helperText;
        }, [value, label, placeholder, name, variant, min, max, disabled, readonly, error, helperText]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiDatePickerEvents.CHANGE, fn);
            return () => el.removeEventListener(UiDatePickerEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-date-picker ref={elRef} {...htmlProps}>
                {children}
            </ui-date-picker>
        );
    },
);

UiDatePicker.displayName = 'UiDatePicker';
