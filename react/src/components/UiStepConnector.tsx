// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/stepper/ui-stepper.ts

import React from 'react';
import type { UiStepConnector as UiStepConnectorElement } from '../../../src/stepper/ui-stepper.js';
// Register the custom element
import '../../../src/stepper/ui-stepper.js';

export interface UiStepConnectorProps extends React.HTMLAttributes<HTMLElement> {
    orientation?: 'horizontal' | 'vertical';
    completed?: boolean;
}

export const UiStepConnector = React.forwardRef<UiStepConnectorElement, UiStepConnectorProps>(
    function UiStepConnector(
        { orientation, completed, children, ...htmlProps }: UiStepConnectorProps,
        ref: React.ForwardedRef<UiStepConnectorElement>,
    ) {
        const innerRef = React.useRef<UiStepConnectorElement>(null);
        const elRef = (ref as React.RefObject<UiStepConnectorElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (orientation !== undefined) (el as unknown as UiStepConnectorElement).orientation = orientation;
        if (completed !== undefined) (el as unknown as UiStepConnectorElement).completed = completed;
        }, [orientation, completed]);

        return (
            <ui-step-connector ref={elRef} {...htmlProps}>
                {children}
            </ui-step-connector>
        );
    },
);

UiStepConnector.displayName = 'UiStepConnector';
