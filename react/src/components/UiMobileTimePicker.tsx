// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/time-picker/ui-time-picker.ts

import React from 'react';
import type { UiMobileTimePicker as UiMobileTimePickerElement } from '../../../src/time-picker/ui-time-picker.js';
import { UiMobileTimePickerEvents } from '../events/ui-mobile-time-picker.js';
// Register the custom element
import '../../../src/time-picker/ui-time-picker.js';

export interface UiMobileTimePickerProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    label?: string;
    ampm?: boolean;
    seconds?: boolean;
    disabled?: boolean;
    error?: boolean;
    helperText?: string;
    onChange?: (event: CustomEvent) => void;
}

export const UiMobileTimePicker = React.forwardRef<UiMobileTimePickerElement, UiMobileTimePickerProps>(
    function UiMobileTimePicker(
        { value, label, ampm, seconds, disabled, error, helperText, onChange, children, ...htmlProps }: UiMobileTimePickerProps,
        ref: React.ForwardedRef<UiMobileTimePickerElement>,
    ) {
        const innerRef = React.useRef<UiMobileTimePickerElement>(null);
        const elRef = (ref as React.RefObject<UiMobileTimePickerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiMobileTimePickerElement).value = value;
        if (label !== undefined) (el as unknown as UiMobileTimePickerElement).label = label;
        if (ampm !== undefined) (el as unknown as UiMobileTimePickerElement).ampm = ampm;
        if (seconds !== undefined) (el as unknown as UiMobileTimePickerElement).seconds = seconds;
        if (disabled !== undefined) (el as unknown as UiMobileTimePickerElement).disabled = disabled;
        if (error !== undefined) (el as unknown as UiMobileTimePickerElement).error = error;
        if (helperText !== undefined) (el as unknown as UiMobileTimePickerElement).helperText = helperText;
        }, [value, label, ampm, seconds, disabled, error, helperText]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiMobileTimePickerEvents.CHANGE, fn);
            return () => el.removeEventListener(UiMobileTimePickerEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-mobile-time-picker ref={elRef} {...htmlProps}>
                {children}
            </ui-mobile-time-picker>
        );
    },
);

UiMobileTimePicker.displayName = 'UiMobileTimePicker';
