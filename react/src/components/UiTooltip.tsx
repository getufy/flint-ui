// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/tooltip/ui-tooltip.ts

import React from 'react';
import type { UiTooltip as UiTooltipElement } from '../../../src/tooltip/ui-tooltip.js';
// Register the custom element
import '../../../src/tooltip/ui-tooltip.js';

export interface UiTooltipProps extends React.HTMLAttributes<HTMLElement> {
    label?: string;
    placement?: 'top' | 'bottom' | 'left' | 'right';
    arrow?: boolean;
    disabled?: boolean;
}

export const UiTooltip = React.forwardRef<UiTooltipElement, UiTooltipProps>(
    function UiTooltip(
        { label, placement, arrow, disabled, children, ...htmlProps }: UiTooltipProps,
        ref: React.ForwardedRef<UiTooltipElement>,
    ) {
        const innerRef = React.useRef<UiTooltipElement>(null);
        const elRef = (ref as React.RefObject<UiTooltipElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (label !== undefined) (el as unknown as UiTooltipElement).label = label;
        if (placement !== undefined) (el as unknown as UiTooltipElement).placement = placement;
        if (arrow !== undefined) (el as unknown as UiTooltipElement).arrow = arrow;
        if (disabled !== undefined) (el as unknown as UiTooltipElement).disabled = disabled;
        }, [label, placement, arrow, disabled]);

        return (
            <ui-tooltip ref={elRef} {...htmlProps}>
                {children}
            </ui-tooltip>
        );
    },
);

UiTooltip.displayName = 'UiTooltip';
