// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/time-picker/ui-time-picker.ts

import React from 'react';
import type { UiStaticTimePicker as UiStaticTimePickerElement } from '../../../src/time-picker/ui-time-picker.js';
import { UiStaticTimePickerEvents } from '../events/ui-static-time-picker.js';
// Register the custom element
import '../../../src/time-picker/ui-time-picker.js';

export interface UiStaticTimePickerProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    ampm?: boolean;
    seconds?: boolean;
    onChange?: (event: CustomEvent) => void;
}

export const UiStaticTimePicker = React.forwardRef<UiStaticTimePickerElement, UiStaticTimePickerProps>(
    function UiStaticTimePicker(
        { value, ampm, seconds, onChange, children, ...htmlProps }: UiStaticTimePickerProps,
        ref: React.ForwardedRef<UiStaticTimePickerElement>,
    ) {
        const innerRef = React.useRef<UiStaticTimePickerElement>(null);
        const elRef = (ref as React.RefObject<UiStaticTimePickerElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiStaticTimePickerElement).value = value;
        if (ampm !== undefined) (el as unknown as UiStaticTimePickerElement).ampm = ampm;
        if (seconds !== undefined) (el as unknown as UiStaticTimePickerElement).seconds = seconds;
        }, [value, ampm, seconds]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onChange) return;
            const fn = onChange as EventListener;
            el.addEventListener(UiStaticTimePickerEvents.CHANGE, fn);
            return () => el.removeEventListener(UiStaticTimePickerEvents.CHANGE, fn);
        }, [onChange]);
    
        return (
            <ui-static-time-picker ref={elRef} {...htmlProps}>
                {children}
            </ui-static-time-picker>
        );
    },
);

UiStaticTimePicker.displayName = 'UiStaticTimePicker';
