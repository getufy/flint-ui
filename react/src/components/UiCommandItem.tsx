// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/command/ui-command.ts

import React from 'react';
import type { UiCommandItem as UiCommandItemElement } from '../../../src/command/ui-command.js';
import { UiCommandItemEvents } from '../events/ui-command-item.js';
// Register the custom element
import '../../../src/command/ui-command.js';

export interface UiCommandItemProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
    disabled?: boolean;
    highlighted?: boolean;
    onUiCommandItemSelect?: (event: CustomEvent) => void;
}

export const UiCommandItem = React.forwardRef<UiCommandItemElement, UiCommandItemProps>(
    function UiCommandItem(
        { value, disabled, highlighted, onUiCommandItemSelect, children, ...htmlProps }: UiCommandItemProps,
        ref: React.ForwardedRef<UiCommandItemElement>,
    ) {
        const innerRef = React.useRef<UiCommandItemElement>(null);
        const elRef = (ref as React.RefObject<UiCommandItemElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiCommandItemElement).value = value;
        if (disabled !== undefined) (el as unknown as UiCommandItemElement).disabled = disabled;
        if (highlighted !== undefined) (el as unknown as UiCommandItemElement).highlighted = highlighted;
        }, [value, disabled, highlighted]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onUiCommandItemSelect) return;
            const fn = onUiCommandItemSelect as EventListener;
            el.addEventListener(UiCommandItemEvents.SELECT, fn);
            return () => el.removeEventListener(UiCommandItemEvents.SELECT, fn);
        }, [onUiCommandItemSelect]);
    
        return (
            <ui-command-item ref={elRef} {...htmlProps}>
                {children}
            </ui-command-item>
        );
    },
);

UiCommandItem.displayName = 'UiCommandItem';
