// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/avatar/ui-avatar.ts

import React from 'react';
import type { UiAvatar as UiAvatarElement } from '../../../src/avatar/ui-avatar.js';
// Register the custom element
import '../../../src/avatar/ui-avatar.js';

export interface UiAvatarProps extends React.HTMLAttributes<HTMLElement> {
    src?: string;
    alt?: string;
    initials?: string;
    variant?: 'circle' | 'square' | 'rounded';
    size?: 'small' | 'medium' | 'large' | 'xlarge';
}

export const UiAvatar = React.forwardRef<UiAvatarElement, UiAvatarProps>(
    function UiAvatar(
        { src, alt, initials, variant, size, children, ...htmlProps }: UiAvatarProps,
        ref: React.ForwardedRef<UiAvatarElement>,
    ) {
        const innerRef = React.useRef<UiAvatarElement>(null);
        const elRef = (ref as React.RefObject<UiAvatarElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (src !== undefined) (el as unknown as UiAvatarElement).src = src;
        if (alt !== undefined) (el as unknown as UiAvatarElement).alt = alt;
        if (initials !== undefined) (el as unknown as UiAvatarElement).initials = initials;
        if (variant !== undefined) (el as unknown as UiAvatarElement).variant = variant;
        if (size !== undefined) (el as unknown as UiAvatarElement).size = size;
        }, [src, alt, initials, variant, size]);

        return (
            <ui-avatar ref={elRef} {...htmlProps}>
                {children}
            </ui-avatar>
        );
    },
);

UiAvatar.displayName = 'UiAvatar';
