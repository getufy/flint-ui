// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/image-list/ui-image-list-item.ts

import React from 'react';
import type { UiImageListItem as UiImageListItemElement } from '../../../src/image-list/ui-image-list-item.js';
// Register the custom element
import '../../../src/image-list/ui-image-list-item.js';

export interface UiImageListItemProps extends React.HTMLAttributes<HTMLElement> {
    rows?: number;
    cols?: number;
    barPosition?: 'overlay' | 'below';
    weave?: 'odd' | 'even';
    aspectRatio?: string;
    fit?: ImageFit;
}

export const UiImageListItem = React.forwardRef<UiImageListItemElement, UiImageListItemProps>(
    function UiImageListItem(
        { rows, cols, barPosition, weave, aspectRatio, fit, children, ...htmlProps }: UiImageListItemProps,
        ref: React.ForwardedRef<UiImageListItemElement>,
    ) {
        const innerRef = React.useRef<UiImageListItemElement>(null);
        const elRef = (ref as React.RefObject<UiImageListItemElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (rows !== undefined) (el as unknown as UiImageListItemElement).rows = rows;
        if (cols !== undefined) (el as unknown as UiImageListItemElement).cols = cols;
        if (barPosition !== undefined) (el as unknown as UiImageListItemElement).barPosition = barPosition;
        if (weave !== undefined) (el as unknown as UiImageListItemElement).weave = weave;
        if (aspectRatio !== undefined) (el as unknown as UiImageListItemElement).aspectRatio = aspectRatio;
        if (fit !== undefined) (el as unknown as UiImageListItemElement).fit = fit;
        }, [rows, cols, barPosition, weave, aspectRatio, fit]);

        return (
            <ui-image-list-item ref={elRef} {...htmlProps}>
                {children}
            </ui-image-list-item>
        );
    },
);

UiImageListItem.displayName = 'UiImageListItem';
