// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/chip/ui-chip.ts

import React from 'react';
import type { UiChip as UiChipElement } from '../../../src/chip/ui-chip.js';
import { UiChipEvents } from '../events/ui-chip.js';
// Register the custom element
import '../../../src/chip/ui-chip.js';

export interface UiChipProps extends React.HTMLAttributes<HTMLElement> {
    label?: string;
    variant?: 'filled' | 'outlined';
    color?: 'default' | 'primary' | 'secondary';
    clickable?: boolean;
    deletable?: boolean;
    disabled?: boolean;
    onClick?: (event: CustomEvent) => void;
    onDelete?: (event: CustomEvent) => void;
}

export const UiChip = React.forwardRef<UiChipElement, UiChipProps>(
    function UiChip(
        { label, variant, color, clickable, deletable, disabled, onClick, onDelete, children, ...htmlProps }: UiChipProps,
        ref: React.ForwardedRef<UiChipElement>,
    ) {
        const innerRef = React.useRef<UiChipElement>(null);
        const elRef = (ref as React.RefObject<UiChipElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (label !== undefined) (el as unknown as UiChipElement).label = label;
        if (variant !== undefined) (el as unknown as UiChipElement).variant = variant;
        if (color !== undefined) (el as unknown as UiChipElement).color = color;
        if (clickable !== undefined) (el as unknown as UiChipElement).clickable = clickable;
        if (deletable !== undefined) (el as unknown as UiChipElement).deletable = deletable;
        if (disabled !== undefined) (el as unknown as UiChipElement).disabled = disabled;
        }, [label, variant, color, clickable, deletable, disabled]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onClick) return;
            const fn = onClick as EventListener;
            el.addEventListener(UiChipEvents.CLICK, fn);
            return () => el.removeEventListener(UiChipEvents.CLICK, fn);
        }, [onClick]);
    
        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onDelete) return;
            const fn = onDelete as EventListener;
            el.addEventListener(UiChipEvents.DELETE, fn);
            return () => el.removeEventListener(UiChipEvents.DELETE, fn);
        }, [onDelete]);
    
        return (
            <ui-chip ref={elRef} {...htmlProps}>
                {children}
            </ui-chip>
        );
    },
);

UiChip.displayName = 'UiChip';
