// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/breadcrumbs/ui-breadcrumbs.ts

import React from 'react';
import type { UiBreadcrumbs as UiBreadcrumbsElement } from '../../../src/breadcrumbs/ui-breadcrumbs.js';
// Register the custom element
import '../../../src/breadcrumbs/ui-breadcrumbs.js';

export interface UiBreadcrumbsProps extends React.HTMLAttributes<HTMLElement> {
    maxItems?: number;
    itemsBefore?: number;
    itemsAfter?: number;
    separator?: string;
}

export const UiBreadcrumbs = React.forwardRef<UiBreadcrumbsElement, UiBreadcrumbsProps>(
    function UiBreadcrumbs(
        { maxItems, itemsBefore, itemsAfter, separator, children, ...htmlProps }: UiBreadcrumbsProps,
        ref: React.ForwardedRef<UiBreadcrumbsElement>,
    ) {
        const innerRef = React.useRef<UiBreadcrumbsElement>(null);
        const elRef = (ref as React.RefObject<UiBreadcrumbsElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (maxItems !== undefined) (el as unknown as UiBreadcrumbsElement).maxItems = maxItems;
        if (itemsBefore !== undefined) (el as unknown as UiBreadcrumbsElement).itemsBefore = itemsBefore;
        if (itemsAfter !== undefined) (el as unknown as UiBreadcrumbsElement).itemsAfter = itemsAfter;
        if (separator !== undefined) (el as unknown as UiBreadcrumbsElement).separator = separator;
        }, [maxItems, itemsBefore, itemsAfter, separator]);

        return (
            <ui-breadcrumbs ref={elRef} {...htmlProps}>
                {children}
            </ui-breadcrumbs>
        );
    },
);

UiBreadcrumbs.displayName = 'UiBreadcrumbs';
