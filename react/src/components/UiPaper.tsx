// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/paper/ui-paper.ts

import React from 'react';
import type { UiPaper as UiPaperElement } from '../../../src/paper/ui-paper.js';
// Register the custom element
import '../../../src/paper/ui-paper.js';

export interface UiPaperProps extends React.HTMLAttributes<HTMLElement> {
    elevation?: number;
    square?: boolean;
    variant?: 'elevated' | 'outlined' | 'flat';
}

export const UiPaper = React.forwardRef<UiPaperElement, UiPaperProps>(
    function UiPaper(
        { elevation, square, variant, children, ...htmlProps }: UiPaperProps,
        ref: React.ForwardedRef<UiPaperElement>,
    ) {
        const innerRef = React.useRef<UiPaperElement>(null);
        const elRef = (ref as React.RefObject<UiPaperElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (elevation !== undefined) (el as unknown as UiPaperElement).elevation = elevation;
        if (square !== undefined) (el as unknown as UiPaperElement).square = square;
        if (variant !== undefined) (el as unknown as UiPaperElement).variant = variant;
        }, [elevation, square, variant]);

        return (
            <ui-paper ref={elRef} {...htmlProps}>
                {children}
            </ui-paper>
        );
    },
);

UiPaper.displayName = 'UiPaper';
