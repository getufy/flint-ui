// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/button/ui-toggle-button.ts

import React from 'react';
import type { UiToggleButton as UiToggleButtonElement } from '../../../src/button/ui-toggle-button.js';
import { UiToggleButtonEvents } from '../events/ui-toggle-button.js';
// Register the custom element
import '../../../src/button/ui-toggle-button.js';

export interface UiToggleButtonProps extends React.HTMLAttributes<HTMLElement> {
    selected?: boolean;
    disabled?: boolean;
    value?: string;
    onToggleClick?: (event: CustomEvent) => void;
}

export const UiToggleButton = React.forwardRef<UiToggleButtonElement, UiToggleButtonProps>(
    function UiToggleButton(
        { selected, disabled, value, onToggleClick, children, ...htmlProps }: UiToggleButtonProps,
        ref: React.ForwardedRef<UiToggleButtonElement>,
    ) {
        const innerRef = React.useRef<UiToggleButtonElement>(null);
        const elRef = (ref as React.RefObject<UiToggleButtonElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (selected !== undefined) (el as unknown as UiToggleButtonElement).selected = selected;
        if (disabled !== undefined) (el as unknown as UiToggleButtonElement).disabled = disabled;
        if (value !== undefined) (el as unknown as UiToggleButtonElement).value = value;
        }, [selected, disabled, value]);

        React.useEffect(() => {
            const el = elRef.current;
            if (!el || !onToggleClick) return;
            const fn = onToggleClick as EventListener;
            el.addEventListener(UiToggleButtonEvents.TOGGLE_CLICK, fn);
            return () => el.removeEventListener(UiToggleButtonEvents.TOGGLE_CLICK, fn);
        }, [onToggleClick]);
    
        return (
            <ui-toggle-button ref={elRef} {...htmlProps}>
                {children}
            </ui-toggle-button>
        );
    },
);

UiToggleButton.displayName = 'UiToggleButton';
