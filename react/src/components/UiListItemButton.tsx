// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/list/ui-list.ts

import React from 'react';
import type { UiListItemButton as UiListItemButtonElement } from '../../../src/list/ui-list.js';
// Register the custom element
import '../../../src/list/ui-list.js';

export interface UiListItemButtonProps extends React.HTMLAttributes<HTMLElement> {
    disabled?: boolean;
    selected?: boolean;
}

export const UiListItemButton = React.forwardRef<UiListItemButtonElement, UiListItemButtonProps>(
    function UiListItemButton(
        { disabled, selected, children, ...htmlProps }: UiListItemButtonProps,
        ref: React.ForwardedRef<UiListItemButtonElement>,
    ) {
        const innerRef = React.useRef<UiListItemButtonElement>(null);
        const elRef = (ref as React.RefObject<UiListItemButtonElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (disabled !== undefined) (el as unknown as UiListItemButtonElement).disabled = disabled;
        if (selected !== undefined) (el as unknown as UiListItemButtonElement).selected = selected;
        }, [disabled, selected]);

        return (
            <ui-list-item-button ref={elRef} {...htmlProps}>
                {children}
            </ui-list-item-button>
        );
    },
);

UiListItemButton.displayName = 'UiListItemButton';
