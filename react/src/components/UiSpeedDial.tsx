// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/speed-dial/ui-speed-dial.ts

import React from 'react';
import type { UiSpeedDial as UiSpeedDialElement } from '../../../src/speed-dial/ui-speed-dial.js';
// Register the custom element
import '../../../src/speed-dial/ui-speed-dial.js';

export interface UiSpeedDialProps extends React.HTMLAttributes<HTMLElement> {
    open?: boolean;
    defaultOpen?: boolean;
    direction?: 'up' | 'down' | 'left' | 'right';
    hidden?: boolean;
    disabled?: boolean;
    persistentTooltips?: boolean;
    closeIcon?: string;
    ariaLabel?: string;
    isTouch?: boolean;
}

export const UiSpeedDial = React.forwardRef<UiSpeedDialElement, UiSpeedDialProps>(
    function UiSpeedDial(
        { open, defaultOpen, direction, hidden, disabled, persistentTooltips, closeIcon, ariaLabel, isTouch, children, ...htmlProps }: UiSpeedDialProps,
        ref: React.ForwardedRef<UiSpeedDialElement>,
    ) {
        const innerRef = React.useRef<UiSpeedDialElement>(null);
        const elRef = (ref as React.RefObject<UiSpeedDialElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (open !== undefined) (el as unknown as UiSpeedDialElement).open = open;
        if (defaultOpen !== undefined) (el as unknown as UiSpeedDialElement).defaultOpen = defaultOpen;
        if (direction !== undefined) (el as unknown as UiSpeedDialElement).direction = direction;
        if (hidden !== undefined) (el as unknown as UiSpeedDialElement).hidden = hidden;
        if (disabled !== undefined) (el as unknown as UiSpeedDialElement).disabled = disabled;
        if (persistentTooltips !== undefined) (el as unknown as UiSpeedDialElement).persistentTooltips = persistentTooltips;
        if (closeIcon !== undefined) (el as unknown as UiSpeedDialElement).closeIcon = closeIcon;
        if (ariaLabel !== undefined) (el as unknown as UiSpeedDialElement).ariaLabel = ariaLabel;
        if (isTouch !== undefined) (el as unknown as UiSpeedDialElement).isTouch = isTouch;
        }, [open, defaultOpen, direction, hidden, disabled, persistentTooltips, closeIcon, ariaLabel, isTouch]);

        return (
            <ui-speed-dial ref={elRef} {...htmlProps}>
                {children}
            </ui-speed-dial>
        );
    },
);

UiSpeedDial.displayName = 'UiSpeedDial';
