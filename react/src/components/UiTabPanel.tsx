// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/tabs/ui-tabs.ts

import React from 'react';
import type { UiTabPanel as UiTabPanelElement } from '../../../src/tabs/ui-tabs.js';
// Register the custom element
import '../../../src/tabs/ui-tabs.js';

export interface UiTabPanelProps extends React.HTMLAttributes<HTMLElement> {
    value?: string;
}

export const UiTabPanel = React.forwardRef<UiTabPanelElement, UiTabPanelProps>(
    function UiTabPanel(
        { value, children, ...htmlProps }: UiTabPanelProps,
        ref: React.ForwardedRef<UiTabPanelElement>,
    ) {
        const innerRef = React.useRef<UiTabPanelElement>(null);
        const elRef = (ref as React.RefObject<UiTabPanelElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (value !== undefined) (el as unknown as UiTabPanelElement).value = value;
        }, [value]);

        return (
            <ui-tab-panel ref={elRef} {...htmlProps}>
                {children}
            </ui-tab-panel>
        );
    },
);

UiTabPanel.displayName = 'UiTabPanel';
