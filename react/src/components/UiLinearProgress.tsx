// Auto-generated by scripts/generate-react-wrappers.ts — do not edit
// Source: src/progress/ui-linear-progress.ts

import React from 'react';
import type { UiLinearProgress as UiLinearProgressElement } from '../../../src/progress/ui-linear-progress.js';
// Register the custom element
import '../../../src/progress/ui-linear-progress.js';

export interface UiLinearProgressProps extends React.HTMLAttributes<HTMLElement> {
    variant?: 'determinate' | 'indeterminate';
    value?: number;
    height?: number;
    color?: 'primary' | 'success' | 'error' | 'warning';
    label?: string;
}

export const UiLinearProgress = React.forwardRef<UiLinearProgressElement, UiLinearProgressProps>(
    function UiLinearProgress(
        { variant, value, height, color, label, children, ...htmlProps }: UiLinearProgressProps,
        ref: React.ForwardedRef<UiLinearProgressElement>,
    ) {
        const innerRef = React.useRef<UiLinearProgressElement>(null);
        const elRef = (ref as React.RefObject<UiLinearProgressElement>) ?? innerRef;

        React.useEffect(() => {
            const el = elRef.current;
            if (!el) return;
        if (variant !== undefined) (el as unknown as UiLinearProgressElement).variant = variant;
        if (value !== undefined) (el as unknown as UiLinearProgressElement).value = value;
        if (height !== undefined) (el as unknown as UiLinearProgressElement).height = height;
        if (color !== undefined) (el as unknown as UiLinearProgressElement).color = color;
        if (label !== undefined) (el as unknown as UiLinearProgressElement).label = label;
        }, [variant, value, height, color, label]);

        return (
            <ui-linear-progress ref={elRef} {...htmlProps}>
                {children}
            </ui-linear-progress>
        );
    },
);

UiLinearProgress.displayName = 'UiLinearProgress';
